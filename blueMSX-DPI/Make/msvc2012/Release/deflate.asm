; Listing generated by Microsoft (R) Optimizing Compiler Version 19.16.27054.0 

	TITLE	D:\repo\MSX-master\MSX\blueMSX-DPI\Src\Unzip\deflate.c
	.686P
	.XMM
	include listing.inc
	.model	flat

INCLUDELIB LIBCMT
INCLUDELIB OLDNAMES

PUBLIC	_deflate_copyright
EXTRN	__length_code:BYTE
EXTRN	__dist_code:BYTE
CONST	SEGMENT
_deflate_copyright DB ' deflate 1.2.3 Copyright 1995-2005 Jean-loup Gaill'
	DB	'y ', 00H
	ORG $+3
_configuration_table DW 00H
	DW	00H
	DW	00H
	DW	00H
	DD	FLAT:_deflate_stored
	DW	04H
	DW	04H
	DW	08H
	DW	04H
	DD	FLAT:_deflate_fast
	DW	04H
	DW	05H
	DW	010H
	DW	08H
	DD	FLAT:_deflate_fast
	DW	04H
	DW	06H
	DW	020H
	DW	020H
	DD	FLAT:_deflate_fast
	DW	04H
	DW	04H
	DW	010H
	DW	010H
	DD	FLAT:_deflate_slow
	DW	08H
	DW	010H
	DW	020H
	DW	020H
	DD	FLAT:_deflate_slow
	DW	08H
	DW	010H
	DW	080H
	DW	080H
	DD	FLAT:_deflate_slow
	DW	08H
	DW	020H
	DW	080H
	DW	0100H
	DD	FLAT:_deflate_slow
	DW	020H
	DW	080H
	DW	0102H
	DW	0400H
	DD	FLAT:_deflate_slow
	DW	020H
	DW	0102H
	DW	0102H
	DW	01000H
	DD	FLAT:_deflate_slow
CONST	ENDS
PUBLIC	_deflate
PUBLIC	_deflateEnd
PUBLIC	_deflateSetDictionary
PUBLIC	_deflateCopy
PUBLIC	_deflateReset
PUBLIC	_deflateParams
PUBLIC	_deflateTune
PUBLIC	_deflateBound
PUBLIC	_deflatePrime
PUBLIC	_deflateSetHeader
PUBLIC	_deflateInit_
PUBLIC	_deflateInit2_
EXTRN	_compressBound:PROC
EXTRN	_adler32:PROC
EXTRN	_crc32:PROC
EXTRN	_zcalloc:PROC
EXTRN	_zcfree:PROC
EXTRN	__tr_init:PROC
EXTRN	__tr_flush_block:PROC
EXTRN	__tr_align:PROC
EXTRN	__tr_stored_block:PROC
EXTRN	_memcpy:PROC
EXTRN	_memset:PROC
EXTRN	_z_errmsg:BYTE
; Function compile flags: /Ogtpy
; File d:\repo\msx-master\msx\bluemsx-dpi\src\unzip\deflate.c
;	COMDAT _longest_match_fast
_TEXT	SEGMENT
_s$ = 8							; size = 4
_cur_match$ = 12					; size = 4
_longest_match_fast PROC				; COMDAT

; 1178 : {

	push	ebx

; 1179 :     register Bytef *scan = s->window + s->strstart; /* current string */
; 1180 :     register Bytef *match;                       /* matched string */
; 1181 :     register int len;                           /* length of current match */
; 1182 :     register Bytef *strend = s->window + s->strstart + MAX_MATCH;
; 1183 : 
; 1184 :     /* The code is optimized for HASH_BITS >= 8 and MAX_MATCH-2 multiple of 16.
; 1185 :      * It is easy to get rid of this optimization if necessary.
; 1186 :      */
; 1187 :     Assert(s->hash_bits >= 8 && MAX_MATCH == 258, "Code too clever");
; 1188 : 
; 1189 :     Assert((ulg)s->strstart <= s->window_size-MIN_LOOKAHEAD, "need lookahead");
; 1190 : 
; 1191 :     Assert(cur_match < s->strstart, "no future");
; 1192 : 
; 1193 :     match = s->window + cur_match;

	mov	ebx, DWORD PTR _cur_match$[esp]
	push	esi
	push	edi
	mov	edi, DWORD PTR _s$[esp+8]
	mov	edx, DWORD PTR [edi+56]
	mov	ecx, DWORD PTR [edi+108]
	add	ecx, edx
	add	edx, ebx

; 1194 : 
; 1195 :     /* Return failure if the match length is less than 2:
; 1196 :      */
; 1197 :     if (match[0] != scan[0] || match[1] != scan[1]) return MIN_MATCH-1;

	mov	al, BYTE PTR [edx]
	lea	esi, DWORD PTR [ecx+258]
	cmp	al, BYTE PTR [ecx]
	jne	SHORT $LN6@longest_ma
	mov	al, BYTE PTR [edx+1]
	cmp	al, BYTE PTR [ecx+1]
	jne	SHORT $LN6@longest_ma

; 1198 : 
; 1199 :     /* The check at best_len-1 can be removed because it will be made
; 1200 :      * again later. (This heuristic is not always a win.)
; 1201 :      * It is not necessary to compare scan[2] and match[2] since they
; 1202 :      * are always equal when the other bytes match, given that
; 1203 :      * the hash keys are equal and that HASH_BITS >= 8.
; 1204 :      */
; 1205 :     scan += 2, match += 2;

	add	ecx, 2
	add	edx, 2
	npad	1
$LL4@longest_ma:

; 1206 :     Assert(*scan == *match, "match[2]?");
; 1207 : 
; 1208 :     /* We check for insufficient lookahead only every 8th comparison;
; 1209 :      * the 256th check will be made at strstart+258.
; 1210 :      */
; 1211 :     do {
; 1212 :     } while (*++scan == *++match && *++scan == *++match &&
; 1213 :              *++scan == *++match && *++scan == *++match &&
; 1214 :              *++scan == *++match && *++scan == *++match &&
; 1215 :              *++scan == *++match && *++scan == *++match &&

	mov	al, BYTE PTR [ecx+1]
	inc	ecx
	cmp	al, BYTE PTR [edx+1]
	jne	SHORT $LN7@longest_ma
	mov	al, BYTE PTR [ecx+1]
	inc	ecx
	cmp	al, BYTE PTR [edx+2]
	jne	SHORT $LN7@longest_ma
	mov	al, BYTE PTR [ecx+1]
	inc	ecx
	cmp	al, BYTE PTR [edx+3]
	jne	SHORT $LN7@longest_ma
	mov	al, BYTE PTR [ecx+1]
	inc	ecx
	cmp	al, BYTE PTR [edx+4]
	jne	SHORT $LN7@longest_ma
	mov	al, BYTE PTR [ecx+1]
	inc	ecx
	cmp	al, BYTE PTR [edx+5]
	jne	SHORT $LN7@longest_ma
	mov	al, BYTE PTR [ecx+1]
	inc	ecx
	cmp	al, BYTE PTR [edx+6]
	jne	SHORT $LN7@longest_ma
	mov	al, BYTE PTR [ecx+1]
	inc	ecx
	cmp	al, BYTE PTR [edx+7]
	jne	SHORT $LN7@longest_ma
	mov	al, BYTE PTR [ecx+1]
	inc	ecx
	add	edx, 8
	cmp	al, BYTE PTR [edx]
	jne	SHORT $LN7@longest_ma
	cmp	ecx, esi
	jb	SHORT $LL4@longest_ma
$LN7@longest_ma:

; 1216 :              scan < strend);
; 1217 : 
; 1218 :     Assert(scan <= s->window+(unsigned)(s->window_size-1), "wild scan");
; 1219 : 
; 1220 :     len = MAX_MATCH - (int)(strend - scan);

	sub	ecx, esi
	add	ecx, 258				; 00000102H

; 1221 : 
; 1222 :     if (len < MIN_MATCH) return MIN_MATCH - 1;

	cmp	ecx, 3
	jl	SHORT $LN6@longest_ma

; 1223 : 
; 1224 :     s->match_start = cur_match;
; 1225 :     return (uInt)len <= s->lookahead ? (uInt)len : s->lookahead;

	mov	eax, DWORD PTR [edi+116]
	cmp	ecx, eax
	mov	DWORD PTR [edi+112], ebx
	pop	edi

; 1226 : }

	pop	esi
	cmovbe	eax, ecx
	pop	ebx
	ret	0
$LN6@longest_ma:
	pop	edi
	pop	esi
	mov	eax, 2
	pop	ebx
	ret	0
_longest_match_fast ENDP
_TEXT	ENDS
; Function compile flags: /Ogtpy
; File d:\repo\msx-master\msx\bluemsx-dpi\src\unzip\deflate.c
;	COMDAT _longest_match
_TEXT	SEGMENT
_scan_end1$1$ = -30					; size = 1
_scan_end$1$ = -29					; size = 1
_nice_match$2$ = -28					; size = 4
_chain_length$1$ = -24					; size = 4
_limit$1$ = -20						; size = 4
tv511 = -16						; size = 4
_wmask$1$ = -12						; size = 4
_prev$1$ = -8						; size = 4
tv502 = -4						; size = 4
_s$ = 8							; size = 4
_cur_match$ = 12					; size = 4
_longest_match PROC					; COMDAT

; 1030 : {

	sub	esp, 32					; 00000020H
	push	ebx
	push	ebp
	push	esi
	push	edi

; 1031 :     unsigned chain_length = s->max_chain_length;/* max hash chain length */

	mov	edi, DWORD PTR _s$[esp+44]

; 1032 :     register Bytef *scan = s->window + s->strstart; /* current string */

	mov	eax, DWORD PTR [edi+56]
	mov	esi, DWORD PTR [edi+108]

; 1033 :     register Bytef *match;                       /* matched string */
; 1034 :     register int len;                           /* length of current match */
; 1035 :     int best_len = s->prev_length;              /* best match length so far */
; 1036 :     int nice_match = s->nice_match;             /* stop if match long enough */
; 1037 :     IPos limit = s->strstart > (IPos)MAX_DIST(s) ?

	mov	ecx, DWORD PTR [edi+44]
	mov	edx, DWORD PTR [edi+124]
	mov	ebp, DWORD PTR [edi+120]
	mov	DWORD PTR tv511[esp+48], eax
	lea	ebx, DWORD PTR [eax+esi]
	mov	eax, DWORD PTR [edi+144]
	mov	DWORD PTR _nice_match$2$[esp+48], eax
	lea	eax, DWORD PTR [ecx-262]
	cmp	esi, eax
	jbe	SHORT $LN18@longest_ma
	sub	esi, ecx
	add	esi, 262				; 00000106H
	mov	DWORD PTR _limit$1$[esp+48], esi
	jmp	SHORT $LN19@longest_ma
$LN18@longest_ma:
	mov	DWORD PTR _limit$1$[esp+48], 0
$LN19@longest_ma:

; 1038 :         s->strstart - (IPos)MAX_DIST(s) : NIL;
; 1039 :     /* Stop when cur_match becomes <= limit. To simplify the code,
; 1040 :      * we prevent matches with the string of window index 0.
; 1041 :      */
; 1042 :     Posf *prev = s->prev;

	mov	eax, DWORD PTR [edi+64]

; 1043 :     uInt wmask = s->w_mask;
; 1044 : 
; 1045 : #ifdef UNALIGNED_OK
; 1046 :     /* Compare two bytes at a time. Note: this is not always beneficial.
; 1047 :      * Try with and without -DUNALIGNED_OK to check.
; 1048 :      */
; 1049 :     register Bytef *strend = s->window + s->strstart + MAX_MATCH - 1;
; 1050 :     register ush scan_start = *(ushf*)scan;
; 1051 :     register ush scan_end   = *(ushf*)(scan+best_len-1);
; 1052 : #else
; 1053 :     register Bytef *strend = s->window + s->strstart + MAX_MATCH;

	lea	esi, DWORD PTR [ebx+258]

; 1054 :     register Byte scan_end1  = scan[best_len-1];
; 1055 :     register Byte scan_end   = scan[best_len];
; 1056 : #endif
; 1057 : 
; 1058 :     /* The code is optimized for HASH_BITS >= 8 and MAX_MATCH-2 multiple of 16.
; 1059 :      * It is easy to get rid of this optimization if necessary.
; 1060 :      */
; 1061 :     Assert(s->hash_bits >= 8 && MAX_MATCH == 258, "Code too clever");
; 1062 : 
; 1063 :     /* Do not waste too much time if we already have a good match: */
; 1064 :     if (s->prev_length >= s->good_match) {
; 1065 :         chain_length >>= 2;
; 1066 :     }
; 1067 :     /* Do not look for matches beyond the end of the input. This is necessary
; 1068 :      * to make deflate deterministic.
; 1069 :      */
; 1070 :     if ((uInt)nice_match > s->lookahead) nice_match = s->lookahead;

	mov	ecx, edx
	mov	DWORD PTR _prev$1$[esp+48], eax
	mov	eax, DWORD PTR [edi+52]
	shr	ecx, 2
	cmp	ebp, DWORD PTR [edi+140]
	mov	DWORD PTR _wmask$1$[esp+48], eax
	mov	al, BYTE PTR [ebx+ebp-1]
	cmovb	ecx, edx
	mov	edx, DWORD PTR [edi+116]
	mov	edi, DWORD PTR _nice_match$2$[esp+48]
	cmp	edi, edx
	mov	ah, BYTE PTR [ebx+ebp]
	cmova	edi, edx
	mov	BYTE PTR _scan_end1$1$[esp+48], al
	mov	DWORD PTR _nice_match$2$[esp+48], edi

; 1071 : 
; 1072 :     Assert((ulg)s->strstart <= s->window_size-MIN_LOOKAHEAD, "need lookahead");
; 1073 : 
; 1074 :     do {
; 1075 :         Assert(cur_match < s->strstart, "no future");
; 1076 :         match = s->window + cur_match;

	mov	edi, DWORD PTR _cur_match$[esp+44]
	mov	BYTE PTR _scan_end$1$[esp+48], ah
	mov	DWORD PTR _chain_length$1$[esp+48], ecx
	mov	DWORD PTR tv502[esp+48], edx
	npad	8
$LL4@longest_ma:
	mov	edx, DWORD PTR tv511[esp+48]
	add	edx, edi

; 1077 : 
; 1078 :         /* Skip to next match if the match length cannot increase
; 1079 :          * or if the match length is less than 2.  Note that the checks below
; 1080 :          * for insufficient lookahead only occur occasionally for performance
; 1081 :          * reasons.  Therefore uninitialized memory will be accessed, and
; 1082 :          * conditional jumps will be made that depend on those values.
; 1083 :          * However the length of the match is limited to the lookahead, so
; 1084 :          * the output of deflate is not affected by the uninitialized values.
; 1085 :          */
; 1086 : #if (defined(UNALIGNED_OK) && MAX_MATCH == 258)
; 1087 :         /* This code assumes sizeof(unsigned short) == 2. Do not use
; 1088 :          * UNALIGNED_OK if your compiler uses a different size.
; 1089 :          */
; 1090 :         if (*(ushf*)(match+best_len-1) != scan_end ||
; 1091 :             *(ushf*)match != scan_start) continue;
; 1092 : 
; 1093 :         /* It is not necessary to compare scan[2] and match[2] since they are
; 1094 :          * always equal when the other bytes match, given that the hash keys
; 1095 :          * are equal and that HASH_BITS >= 8. Compare 2 bytes at a time at
; 1096 :          * strstart+3, +5, ... up to strstart+257. We check for insufficient
; 1097 :          * lookahead only every 4th comparison; the 128th check will be made
; 1098 :          * at strstart+257. If MAX_MATCH-2 is not a multiple of 8, it is
; 1099 :          * necessary to put more guard bytes at the end of the window, or
; 1100 :          * to check more often for insufficient lookahead.
; 1101 :          */
; 1102 :         Assert(scan[2] == match[2], "scan[2]?");
; 1103 :         scan++, match++;
; 1104 :         do {
; 1105 :         } while (*(ushf*)(scan+=2) == *(ushf*)(match+=2) &&
; 1106 :                  *(ushf*)(scan+=2) == *(ushf*)(match+=2) &&
; 1107 :                  *(ushf*)(scan+=2) == *(ushf*)(match+=2) &&
; 1108 :                  *(ushf*)(scan+=2) == *(ushf*)(match+=2) &&
; 1109 :                  scan < strend);
; 1110 :         /* The funny "do {}" generates better code on most compilers */
; 1111 : 
; 1112 :         /* Here, scan <= window+strstart+257 */
; 1113 :         Assert(scan <= s->window+(unsigned)(s->window_size-1), "wild scan");
; 1114 :         if (*scan == *match) scan++;
; 1115 : 
; 1116 :         len = (MAX_MATCH - 1) - (int)(strend-scan);
; 1117 :         scan = strend - (MAX_MATCH-1);
; 1118 : 
; 1119 : #else /* UNALIGNED_OK */
; 1120 : 
; 1121 :         if (match[best_len]   != scan_end  ||
; 1122 :             match[best_len-1] != scan_end1 ||
; 1123 :             *match            != *scan     ||

	cmp	BYTE PTR [edx+ebp], ah
	jne	$LN2@longest_ma
	cmp	BYTE PTR [edx+ebp-1], al
	jne	$LN2@longest_ma
	mov	al, BYTE PTR [edx]
	cmp	al, BYTE PTR [ebx]
	jne	$LN2@longest_ma
	mov	al, BYTE PTR [edx+1]
	cmp	al, BYTE PTR [ebx+1]
	jne	$LN2@longest_ma

; 1124 :             *++match          != scan[1])      continue;
; 1125 : 
; 1126 :         /* The check at best_len-1 can be removed because it will be made
; 1127 :          * again later. (This heuristic is not always a win.)
; 1128 :          * It is not necessary to compare scan[2] and match[2] since they
; 1129 :          * are always equal when the other bytes match, given that
; 1130 :          * the hash keys are equal and that HASH_BITS >= 8.
; 1131 :          */
; 1132 :         scan += 2, match++;

	lea	eax, DWORD PTR [ebx+2]
	add	edx, 2
$LL7@longest_ma:

; 1133 :         Assert(*scan == *match, "match[2]?");
; 1134 : 
; 1135 :         /* We check for insufficient lookahead only every 8th comparison;
; 1136 :          * the 256th check will be made at strstart+258.
; 1137 :          */
; 1138 :         do {
; 1139 :         } while (*++scan == *++match && *++scan == *++match &&
; 1140 :                  *++scan == *++match && *++scan == *++match &&
; 1141 :                  *++scan == *++match && *++scan == *++match &&
; 1142 :                  *++scan == *++match && *++scan == *++match &&

	mov	cl, BYTE PTR [eax+1]
	inc	eax
	cmp	cl, BYTE PTR [edx+1]
	jne	SHORT $LN12@longest_ma
	mov	cl, BYTE PTR [eax+1]
	inc	eax
	cmp	cl, BYTE PTR [edx+2]
	jne	SHORT $LN12@longest_ma
	mov	cl, BYTE PTR [eax+1]
	inc	eax
	cmp	cl, BYTE PTR [edx+3]
	jne	SHORT $LN12@longest_ma
	mov	cl, BYTE PTR [eax+1]
	inc	eax
	cmp	cl, BYTE PTR [edx+4]
	jne	SHORT $LN12@longest_ma
	mov	cl, BYTE PTR [eax+1]
	inc	eax
	cmp	cl, BYTE PTR [edx+5]
	jne	SHORT $LN12@longest_ma
	mov	cl, BYTE PTR [eax+1]
	inc	eax
	cmp	cl, BYTE PTR [edx+6]
	jne	SHORT $LN12@longest_ma
	mov	cl, BYTE PTR [eax+1]
	inc	eax
	cmp	cl, BYTE PTR [edx+7]
	jne	SHORT $LN12@longest_ma
	mov	cl, BYTE PTR [eax+1]
	inc	eax
	add	edx, 8
	cmp	cl, BYTE PTR [edx]
	jne	SHORT $LN12@longest_ma
	cmp	eax, esi
	jb	SHORT $LL7@longest_ma
$LN12@longest_ma:

; 1143 :                  scan < strend);
; 1144 : 
; 1145 :         Assert(scan <= s->window+(unsigned)(s->window_size-1), "wild scan");
; 1146 : 
; 1147 :         len = MAX_MATCH - (int)(strend - scan);

	sub	eax, esi
	add	eax, 258				; 00000102H

; 1148 :         scan = strend - MAX_MATCH;
; 1149 : 
; 1150 : #endif /* UNALIGNED_OK */
; 1151 : 
; 1152 :         if (len > best_len) {

	cmp	eax, ebp
	jle	SHORT $LN29@longest_ma

; 1153 :             s->match_start = cur_match;
; 1154 :             best_len = len;

	mov	ebp, eax
	mov	ecx, DWORD PTR _s$[esp+44]
	mov	DWORD PTR [ecx+112], edi

; 1155 :             if (len >= nice_match) break;

	cmp	eax, DWORD PTR _nice_match$2$[esp+48]
	jge	SHORT $LN3@longest_ma

; 1156 : #ifdef UNALIGNED_OK
; 1157 :             scan_end = *(ushf*)(scan+best_len-1);
; 1158 : #else
; 1159 :             scan_end1  = scan[best_len-1];

	mov	cl, BYTE PTR [eax+ebx-1]

; 1160 :             scan_end   = scan[best_len];

	mov	al, BYTE PTR [eax+ebx]
	mov	BYTE PTR _scan_end1$1$[esp+48], cl
	mov	BYTE PTR _scan_end$1$[esp+48], al
$LN29@longest_ma:

; 1161 : #endif
; 1162 :         }
; 1163 :     } while ((cur_match = prev[cur_match & wmask]) > limit
; 1164 :              && --chain_length != 0);

	mov	ecx, DWORD PTR _chain_length$1$[esp+48]
$LN2@longest_ma:
	mov	eax, DWORD PTR _wmask$1$[esp+48]
	mov	edx, DWORD PTR _prev$1$[esp+48]
	and	eax, edi
	movzx	edi, WORD PTR [edx+eax*2]
	cmp	edi, DWORD PTR _limit$1$[esp+48]
	jbe	SHORT $LN3@longest_ma
	mov	al, BYTE PTR _scan_end1$1$[esp+48]
	sub	ecx, 1
	mov	ah, BYTE PTR _scan_end$1$[esp+48]
	mov	DWORD PTR _chain_length$1$[esp+48], ecx
	jne	$LL4@longest_ma
$LN3@longest_ma:

; 1165 : 
; 1166 :     if ((uInt)best_len <= s->lookahead) return (uInt)best_len;

	mov	eax, DWORD PTR tv502[esp+48]
	cmp	ebp, eax
	pop	edi
	pop	esi
	cmovbe	eax, ebp
	pop	ebp
	pop	ebx

; 1167 :     return s->lookahead;
; 1168 : }

	add	esp, 32					; 00000020H
	ret	0
_longest_match ENDP
_TEXT	ENDS
; Function compile flags: /Ogtpy
; File d:\repo\msx-master\msx\bluemsx-dpi\src\unzip\deflate.c
;	COMDAT _read_buf
_TEXT	SEGMENT
_strm$ = 8						; size = 4
_buf$ = 12						; size = 4
_size$ = 16						; size = 4
_read_buf PROC						; COMDAT

; 960  : {

	push	esi

; 961  :     unsigned len = strm->avail_in;

	mov	esi, DWORD PTR _strm$[esp]
	push	edi
	mov	ecx, DWORD PTR [esi+4]

; 962  : 
; 963  :     if (len > size) len = size;

	mov	edi, ecx
	cmp	ecx, DWORD PTR _size$[esp+4]
	cmova	edi, DWORD PTR _size$[esp+4]

; 964  :     if (len == 0) return 0;

	test	edi, edi
	jne	SHORT $LN3@read_buf
	pop	edi
	xor	eax, eax

; 981  : }

	pop	esi
	ret	0
$LN3@read_buf:

; 965  : 
; 966  :     strm->avail_in  -= len;
; 967  : 
; 968  :     if (strm->state->wrap == 1) {

	mov	eax, DWORD PTR [esi+28]
	sub	ecx, edi
	mov	DWORD PTR [esi+4], ecx
	mov	eax, DWORD PTR [eax+24]
	cmp	eax, 1
	jne	SHORT $LN4@read_buf

; 969  :         strm->adler = adler32(strm->adler, strm->next_in, len);

	push	edi
	push	DWORD PTR [esi]
	push	DWORD PTR [esi+48]
	call	_adler32

; 970  :     }

	jmp	SHORT $LN8@read_buf
$LN4@read_buf:

; 971  : #ifdef GZIP
; 972  :     else if (strm->state->wrap == 2) {

	cmp	eax, 2
	jne	SHORT $LN6@read_buf

; 973  :         strm->adler = crc32(strm->adler, strm->next_in, len);

	push	edi
	push	DWORD PTR [esi]
	push	DWORD PTR [esi+48]
	call	_crc32
$LN8@read_buf:

; 974  :     }
; 975  : #endif
; 976  :     zmemcpy(buf, strm->next_in, len);

	mov	DWORD PTR [esi+48], eax
	add	esp, 12					; 0000000cH
$LN6@read_buf:
	push	edi
	push	DWORD PTR [esi]
	push	DWORD PTR _buf$[esp+12]
	call	_memcpy

; 977  :     strm->next_in  += len;

	add	DWORD PTR [esi], edi
	add	esp, 12					; 0000000cH

; 978  :     strm->total_in += len;

	add	DWORD PTR [esi+8], edi

; 979  : 
; 980  :     return (int)len;

	mov	eax, edi
	pop	edi

; 981  : }

	pop	esi
	ret	0
_read_buf ENDP
_TEXT	ENDS
; Function compile flags: /Ogtpy
; File d:\repo\msx-master\msx\bluemsx-dpi\src\unzip\deflate.c
;	COMDAT _flush_pending
_TEXT	SEGMENT
_strm$ = 8						; size = 4
_flush_pending PROC					; COMDAT

; 534  : {

	push	esi
	push	edi

; 535  :     unsigned len = strm->state->pending;

	mov	edi, DWORD PTR _strm$[esp+4]
	mov	ecx, DWORD PTR [edi+28]

; 536  : 
; 537  :     if (len > strm->avail_out) len = strm->avail_out;

	mov	esi, DWORD PTR [edi+16]

; 538  :     if (len == 0) return;

	cmp	DWORD PTR [ecx+20], esi
	cmovbe	esi, DWORD PTR [ecx+20]
	test	esi, esi
	je	SHORT $LN4@flush_pend

; 539  : 
; 540  :     zmemcpy(strm->next_out, strm->state->pending_out, len);

	push	esi
	push	DWORD PTR [ecx+16]
	push	DWORD PTR [edi+12]
	call	_memcpy

; 541  :     strm->next_out  += len;
; 542  :     strm->state->pending_out  += len;

	mov	eax, DWORD PTR [edi+28]
	add	esp, 12					; 0000000cH
	add	DWORD PTR [edi+12], esi
	add	DWORD PTR [eax+16], esi

; 543  :     strm->total_out += len;
; 544  :     strm->avail_out  -= len;
; 545  :     strm->state->pending -= len;

	mov	eax, DWORD PTR [edi+28]
	add	DWORD PTR [edi+20], esi
	sub	DWORD PTR [edi+16], esi
	sub	DWORD PTR [eax+20], esi

; 546  :     if (strm->state->pending == 0) {

	mov	ecx, DWORD PTR [edi+28]
	cmp	DWORD PTR [ecx+20], 0
	jne	SHORT $LN4@flush_pend

; 547  :         strm->state->pending_out = strm->state->pending_buf;

	mov	eax, DWORD PTR [ecx+8]
	mov	DWORD PTR [ecx+16], eax
$LN4@flush_pend:

; 548  :     }
; 549  : }

	pop	edi
	pop	esi
	ret	0
_flush_pending ENDP
_TEXT	ENDS
; Function compile flags: /Ogtpy
; File d:\repo\msx-master\msx\bluemsx-dpi\src\unzip\deflate.c
;	COMDAT _putShortMSB
_TEXT	SEGMENT
_s$ = 8							; size = 4
_b$ = 12						; size = 4
_putShortMSB PROC					; COMDAT

; 521  : {

	push	ebx

; 522  :     put_byte(s, (Byte)(b >> 8));

	mov	ebx, DWORD PTR _b$[esp]
	mov	edx, ebx
	push	esi
	mov	esi, DWORD PTR _s$[esp+4]
	shr	edx, 8
	mov	eax, DWORD PTR [esi+20]
	mov	ecx, DWORD PTR [esi+8]
	mov	BYTE PTR [ecx+eax], dl
	inc	DWORD PTR [esi+20]
	mov	ecx, DWORD PTR [esi+20]

; 523  :     put_byte(s, (Byte)(b & 0xff));

	mov	eax, DWORD PTR [esi+8]
	mov	BYTE PTR [ecx+eax], bl
	inc	DWORD PTR [esi+20]
	pop	esi
	pop	ebx

; 524  : }

	ret	0
_putShortMSB ENDP
_TEXT	ENDS
; Function compile flags: /Ogtpy
; File d:\repo\msx-master\msx\bluemsx-dpi\src\unzip\deflate.c
;	COMDAT _lm_init
_TEXT	SEGMENT
_s$ = 8							; size = 4
_lm_init PROC						; COMDAT

; 988  : {

	push	esi

; 989  :     s->window_size = (ulg)2L*s->w_size;

	mov	esi, DWORD PTR _s$[esp]

; 990  : 
; 991  :     CLEAR_HASH(s);

	xor	edx, edx
	mov	eax, DWORD PTR [esi+44]
	mov	ecx, DWORD PTR [esi+76]
	add	eax, eax
	mov	DWORD PTR [esi+60], eax
	mov	eax, DWORD PTR [esi+68]
	mov	WORD PTR [eax+ecx*2-2], dx
	mov	eax, DWORD PTR [esi+76]
	lea	eax, DWORD PTR [eax*2-2]
	push	eax
	push	edx
	push	DWORD PTR [esi+68]
	call	_memset

; 992  : 
; 993  :     /* Set the default configuration parameters:
; 994  :      */
; 995  :     s->max_lazy_match   = configuration_table[s->level].max_lazy;

	mov	eax, DWORD PTR [esi+132]
	add	esp, 12					; 0000000cH
	lea	ecx, DWORD PTR [eax+eax*2]
	movzx	eax, WORD PTR _configuration_table[ecx*4+2]
	mov	DWORD PTR [esi+128], eax

; 996  :     s->good_match       = configuration_table[s->level].good_length;

	movzx	eax, WORD PTR _configuration_table[ecx*4]
	mov	DWORD PTR [esi+140], eax

; 997  :     s->nice_match       = configuration_table[s->level].nice_length;

	movzx	eax, WORD PTR _configuration_table[ecx*4+4]
	mov	DWORD PTR [esi+144], eax

; 998  :     s->max_chain_length = configuration_table[s->level].max_chain;

	movzx	eax, WORD PTR _configuration_table[ecx*4+6]
	mov	DWORD PTR [esi+124], eax

; 999  : 
; 1000 :     s->strstart = 0;

	mov	DWORD PTR [esi+108], 0

; 1001 :     s->block_start = 0L;

	mov	DWORD PTR [esi+92], 0

; 1002 :     s->lookahead = 0;

	mov	DWORD PTR [esi+116], 0

; 1003 :     s->match_length = s->prev_length = MIN_MATCH-1;

	mov	DWORD PTR [esi+120], 2
	mov	DWORD PTR [esi+96], 2

; 1004 :     s->match_available = 0;

	mov	DWORD PTR [esi+104], 0

; 1005 :     s->ins_h = 0;

	mov	DWORD PTR [esi+72], 0
	pop	esi

; 1006 : #ifndef FASTEST
; 1007 : #ifdef ASMV
; 1008 :     match_init(); /* initialize the asm code */
; 1009 : #endif
; 1010 : #endif
; 1011 : }

	ret	0
_lm_init ENDP
_TEXT	ENDS
; Function compile flags: /Ogtpy
; File d:\repo\msx-master\msx\bluemsx-dpi\src\unzip\deflate.c
;	COMDAT _deflate_slow
_TEXT	SEGMENT
_hash_head$1$ = -8					; size = 4
tv1946 = -4						; size = 4
tv1941 = 8						; size = 4
_s$ = 8							; size = 4
_flush$ = 12						; size = 4
_deflate_slow PROC					; COMDAT

; 1557 : {

	sub	esp, 8
	push	ebx
	push	ebp

; 1558 :     IPos hash_head = NIL;    /* head of hash chain */

	mov	ebp, DWORD PTR _flush$[esp+12]
	push	esi
	mov	esi, DWORD PTR _s$[esp+16]
	push	edi
	xor	edi, edi
	mov	DWORD PTR _hash_head$1$[esp+24], edi
$LL2@deflate_sl:

; 1559 :     int bflush;              /* set if current block must be flushed */
; 1560 : 
; 1561 :     /* Process the input block. */
; 1562 :     for (;;) {
; 1563 :         /* Make sure that we always have enough lookahead, except
; 1564 :          * at the end of the input file. We need MAX_MATCH bytes
; 1565 :          * for the next match, plus MIN_MATCH bytes to insert the
; 1566 :          * string following the next match.
; 1567 :          */
; 1568 :         if (s->lookahead < MIN_LOOKAHEAD) {

	mov	eax, DWORD PTR [esi+116]
	cmp	eax, 262				; 00000106H
	jae	SHORT $LN10@deflate_sl

; 1569 :             fill_window(s);

	push	esi
	call	_fill_window

; 1570 :             if (s->lookahead < MIN_LOOKAHEAD && flush == Z_NO_FLUSH) {

	mov	eax, DWORD PTR [esi+116]
	add	esp, 4
	cmp	eax, 262				; 00000106H
	jae	SHORT $LN9@deflate_sl
	test	ebp, ebp
	je	$LN59@deflate_sl
$LN9@deflate_sl:

; 1571 :                 return need_more;
; 1572 :             }
; 1573 :             if (s->lookahead == 0) break; /* flush the current block */

	test	eax, eax
	je	$LN57@deflate_sl
$LN10@deflate_sl:

; 1574 :         }
; 1575 : 
; 1576 :         /* Insert the string window[strstart .. strstart+2] in the
; 1577 :          * dictionary, and set hash_head to the head of the hash chain:
; 1578 :          */
; 1579 :         if (s->lookahead >= MIN_MATCH) {

	cmp	eax, 3
	jb	SHORT $LN11@deflate_sl

; 1580 :             INSERT_STRING(s, s->strstart, hash_head);

	mov	edi, DWORD PTR [esi+108]
	mov	eax, DWORD PTR [esi+56]
	mov	ebx, DWORD PTR [esi+72]
	mov	ecx, DWORD PTR [esi+88]
	mov	edx, DWORD PTR [esi+52]
	movzx	eax, BYTE PTR [eax+edi+2]
	and	edx, edi
	shl	ebx, cl
	mov	ecx, DWORD PTR [esi+64]
	xor	ebx, eax
	and	ebx, DWORD PTR [esi+84]
	mov	eax, DWORD PTR [esi+68]
	mov	DWORD PTR [esi+72], ebx
	mov	ax, WORD PTR [eax+ebx*2]
	mov	WORD PTR [ecx+edx*2], ax
	mov	ecx, DWORD PTR [esi+52]
	mov	edx, DWORD PTR [esi+108]
	and	ecx, edx
	mov	eax, DWORD PTR [esi+64]
	movzx	edi, WORD PTR [eax+ecx*2]
	mov	ecx, DWORD PTR [esi+72]
	mov	eax, DWORD PTR [esi+68]
	mov	DWORD PTR _hash_head$1$[esp+24], edi
	mov	WORD PTR [eax+ecx*2], dx
$LN11@deflate_sl:

; 1581 :         }
; 1582 : 
; 1583 :         /* Find the longest match, discarding those <= prev_length.
; 1584 :          */
; 1585 :         s->prev_length = s->match_length, s->prev_match = s->match_start;

	mov	edx, DWORD PTR [esi+96]

; 1586 :         s->match_length = MIN_MATCH-1;

	mov	ecx, 2
	mov	eax, DWORD PTR [esi+112]
	mov	DWORD PTR [esi+120], edx
	mov	DWORD PTR [esi+100], eax
	mov	DWORD PTR [esi+96], 2

; 1587 : 
; 1588 :         if (hash_head != NIL && s->prev_length < s->max_lazy_match &&

	test	edi, edi
	je	SHORT $LN16@deflate_sl
	cmp	edx, DWORD PTR [esi+128]
	jae	SHORT $LN16@deflate_sl
	mov	edx, DWORD PTR [esi+108]
	mov	eax, DWORD PTR [esi+44]
	sub	edx, edi
	sub	eax, 262				; 00000106H
	cmp	edx, eax
	ja	SHORT $LN16@deflate_sl

; 1589 :             s->strstart - hash_head <= MAX_DIST(s)) {
; 1590 :             /* To simplify the code, we prevent matches with the string
; 1591 :              * of window index 0 (in particular we have to avoid a match
; 1592 :              * of the string with itself at the start of the input file).
; 1593 :              */
; 1594 :             if (s->strategy != Z_HUFFMAN_ONLY && s->strategy != Z_RLE) {

	mov	eax, DWORD PTR [esi+136]
	cmp	eax, ecx
	je	SHORT $LN15@deflate_sl
	cmp	eax, 3
	je	SHORT $LN65@deflate_sl

; 1595 :                 s->match_length = longest_match (s, hash_head);

	push	edi
	push	esi
	call	_longest_match
	jmp	SHORT $LN71@deflate_sl
$LN65@deflate_sl:

; 1596 :             } else if (s->strategy == Z_RLE && s->strstart - hash_head == 1) {

	cmp	edx, 1
	jne	SHORT $LN15@deflate_sl

; 1597 :                 s->match_length = longest_match_fast (s, hash_head);

	push	edi
	push	esi
	call	_longest_match_fast
$LN71@deflate_sl:

; 1598 :             }
; 1599 :             /* longest_match() or longest_match_fast() sets match_start */
; 1600 : 
; 1601 :             if (s->match_length <= 5 && (s->strategy == Z_FILTERED

	mov	DWORD PTR [esi+96], eax
	add	esp, 8
$LN15@deflate_sl:
	mov	eax, DWORD PTR [esi+96]
	mov	ecx, eax
	cmp	eax, 5
	ja	SHORT $LN16@deflate_sl
	cmp	DWORD PTR [esi+136], 1
	je	SHORT $LN17@deflate_sl
	cmp	eax, 3
	jne	SHORT $LN16@deflate_sl
	mov	eax, DWORD PTR [esi+108]
	sub	eax, DWORD PTR [esi+112]
	cmp	eax, 4096				; 00001000H
	jbe	SHORT $LN16@deflate_sl
$LN17@deflate_sl:

; 1602 : #if TOO_FAR <= 32767
; 1603 :                 || (s->match_length == MIN_MATCH &&
; 1604 :                     s->strstart - s->match_start > TOO_FAR)
; 1605 : #endif
; 1606 :                 )) {
; 1607 : 
; 1608 :                 /* If prev_match is also MIN_MATCH, match_start is garbage
; 1609 :                  * but we will ignore the current match anyway.
; 1610 :                  */
; 1611 :                 s->match_length = MIN_MATCH-1;

	mov	DWORD PTR [esi+96], 2
	mov	ecx, 2
$LN16@deflate_sl:

; 1612 :             }
; 1613 :         }
; 1614 :         /* If there was a match at the previous step and the current
; 1615 :          * match is not better, output the previous match:
; 1616 :          */
; 1617 :         if (s->prev_length >= MIN_MATCH && s->match_length <= s->prev_length) {

	mov	ebx, DWORD PTR [esi+120]
	cmp	ebx, 3
	jb	$LN62@deflate_sl
	cmp	ecx, ebx
	ja	$LN62@deflate_sl

; 1618 :             uInt max_insert = s->strstart + s->lookahead - MIN_MATCH;

	mov	ebp, DWORD PTR [esi+116]

; 1619 :             /* Do not insert strings in hash table beyond this. */
; 1620 : 
; 1621 :             check_match(s, s->strstart-1, s->prev_match, s->prev_length);
; 1622 : 
; 1623 :             _tr_tally_dist(s, s->strstart -1 - s->prev_match,

	sub	bl, 3
	mov	ecx, DWORD PTR [esi+108]
	add	ebp, -3					; fffffffdH
	mov	eax, DWORD PTR [esi+5796]
	add	ebp, ecx
	sub	cx, WORD PTR [esi+100]
	dec	cx
	movzx	edx, cx
	mov	ecx, DWORD PTR [esi+5792]
	mov	WORD PTR [eax+ecx*2], dx
	add	edx, 65535				; 0000ffffH
	mov	eax, DWORD PTR [esi+5792]
	mov	ecx, DWORD PTR [esi+5784]
	mov	BYTE PTR [ecx+eax], bl
	mov	ecx, 256				; 00000100H
	inc	DWORD PTR [esi+5792]
	movzx	eax, bl
	movzx	eax, BYTE PTR __length_code[eax]
	inc	WORD PTR [esi+eax*4+1176]
	movzx	eax, dx
	cmp	dx, cx
	jae	SHORT $LN30@deflate_sl
	mov	al, BYTE PTR __dist_code[eax]
	jmp	SHORT $LN31@deflate_sl
$LN30@deflate_sl:
	shr	eax, 7
	mov	al, BYTE PTR __dist_code[eax+256]
$LN31@deflate_sl:
	movzx	eax, al
	inc	WORD PTR [esi+eax*4+2440]
	mov	eax, DWORD PTR [esi+5788]

; 1624 :                            s->prev_length - MIN_MATCH, bflush);
; 1625 : 
; 1626 :             /* Insert in hash table all strings up to the end of the match.
; 1627 :              * strstart-1 and strstart are already inserted. If there is not
; 1628 :              * enough lookahead, the last two strings are not inserted in
; 1629 :              * the hash table.
; 1630 :              */
; 1631 :             s->lookahead -= s->prev_length-1;

	mov	ecx, DWORD PTR [esi+120]
	dec	eax
	mov	DWORD PTR tv1946[esp+24], eax
	mov	eax, DWORD PTR [esi+5792]
	mov	DWORD PTR tv1941[esp+20], eax
	mov	eax, 1
	sub	eax, ecx
	add	DWORD PTR [esi+116], eax

; 1632 :             s->prev_length -= 2;

	lea	eax, DWORD PTR [ecx-2]
	mov	DWORD PTR [esi+120], eax
	npad	5
$LL7@deflate_sl:

; 1633 :             do {
; 1634 :                 if (++s->strstart <= max_insert) {

	mov	ebx, DWORD PTR [esi+108]
	inc	ebx
	mov	DWORD PTR [esi+108], ebx
	cmp	ebx, ebp
	ja	SHORT $LN5@deflate_sl

; 1635 :                     INSERT_STRING(s, s->strstart, hash_head);

	mov	eax, DWORD PTR [esi+56]
	mov	edi, DWORD PTR [esi+72]
	mov	ecx, DWORD PTR [esi+88]
	mov	edx, DWORD PTR [esi+52]
	movzx	eax, BYTE PTR [eax+ebx+2]
	and	edx, ebx
	shl	edi, cl
	mov	ecx, DWORD PTR [esi+64]
	xor	edi, eax
	and	edi, DWORD PTR [esi+84]
	mov	eax, DWORD PTR [esi+68]
	mov	DWORD PTR [esi+72], edi
	mov	ax, WORD PTR [eax+edi*2]
	mov	WORD PTR [ecx+edx*2], ax
	mov	ecx, DWORD PTR [esi+52]
	mov	edx, DWORD PTR [esi+108]
	and	ecx, edx
	mov	eax, DWORD PTR [esi+64]
	movzx	edi, WORD PTR [eax+ecx*2]
	mov	ecx, DWORD PTR [esi+72]
	mov	eax, DWORD PTR [esi+68]
	mov	WORD PTR [eax+ecx*2], dx
	mov	ebx, DWORD PTR [esi+108]
$LN5@deflate_sl:

; 1636 :                 }
; 1637 :             } while (--s->prev_length != 0);

	add	DWORD PTR [esi+120], -1
	jne	SHORT $LL7@deflate_sl

; 1638 :             s->match_available = 0;
; 1639 :             s->match_length = MIN_MATCH-1;
; 1640 :             s->strstart++;
; 1641 : 
; 1642 :             if (bflush) FLUSH_BLOCK(s, 0);

	mov	eax, DWORD PTR tv1941[esp+20]
	inc	ebx
	mov	ebp, DWORD PTR _flush$[esp+20]
	mov	DWORD PTR _hash_head$1$[esp+24], edi
	mov	DWORD PTR [esi+104], 0
	mov	DWORD PTR [esi+96], 2
	mov	DWORD PTR [esi+108], ebx
	cmp	eax, DWORD PTR tv1946[esp+24]
	jne	$LL2@deflate_sl
	mov	ecx, DWORD PTR [esi+92]
	test	ecx, ecx
	js	SHORT $LN32@deflate_sl
	mov	eax, DWORD PTR [esi+56]
	add	eax, ecx
	jmp	SHORT $LN33@deflate_sl
$LN32@deflate_sl:
	xor	eax, eax
$LN33@deflate_sl:
	push	0
	sub	ebx, ecx
	push	ebx
	push	eax
	push	esi
	call	__tr_flush_block
	mov	edi, DWORD PTR [esi]
	add	esp, 16					; 00000010H
	mov	eax, DWORD PTR [esi+108]
	mov	DWORD PTR [esi+92], eax

; 535  :     unsigned len = strm->state->pending;

	mov	ecx, DWORD PTR [edi+28]

; 536  : 
; 537  :     if (len > strm->avail_out) len = strm->avail_out;

	mov	ebx, DWORD PTR [edi+16]

; 538  :     if (len == 0) return;

	cmp	DWORD PTR [ecx+20], ebx
	cmovbe	ebx, DWORD PTR [ecx+20]
	test	ebx, ebx
	je	SHORT $LN41@deflate_sl

; 539  : 
; 540  :     zmemcpy(strm->next_out, strm->state->pending_out, len);

	push	ebx
	push	DWORD PTR [ecx+16]
	push	DWORD PTR [edi+12]
	call	_memcpy

; 541  :     strm->next_out  += len;
; 542  :     strm->state->pending_out  += len;

	mov	eax, DWORD PTR [edi+28]
	add	esp, 12					; 0000000cH
	add	DWORD PTR [edi+12], ebx
	add	DWORD PTR [eax+16], ebx

; 543  :     strm->total_out += len;
; 544  :     strm->avail_out  -= len;
; 545  :     strm->state->pending -= len;

	mov	eax, DWORD PTR [edi+28]
	add	DWORD PTR [edi+20], ebx
	sub	DWORD PTR [edi+16], ebx
	sub	DWORD PTR [eax+20], ebx

; 546  :     if (strm->state->pending == 0) {

	mov	ecx, DWORD PTR [edi+28]
	cmp	DWORD PTR [ecx+20], 0
	jne	SHORT $LN41@deflate_sl

; 547  :         strm->state->pending_out = strm->state->pending_buf;

	mov	eax, DWORD PTR [ecx+8]
	mov	DWORD PTR [ecx+16], eax
$LN41@deflate_sl:

; 1638 :             s->match_available = 0;
; 1639 :             s->match_length = MIN_MATCH-1;
; 1640 :             s->strstart++;
; 1641 : 
; 1642 :             if (bflush) FLUSH_BLOCK(s, 0);

	mov	eax, DWORD PTR [esi]
	cmp	DWORD PTR [eax+16], 0
	je	$LN59@deflate_sl
	mov	edi, DWORD PTR _hash_head$1$[esp+24]
	jmp	$LL2@deflate_sl
$LN62@deflate_sl:

; 1643 : 
; 1644 :         } else if (s->match_available) {

	cmp	DWORD PTR [esi+104], 0
	je	$LN23@deflate_sl

; 1645 :             /* If there was no match at the previous position, output a
; 1646 :              * single literal. If there was a match but the current match
; 1647 :              * is longer, truncate the previous match to a single literal.
; 1648 :              */
; 1649 :             Tracevv((stderr,"%c", s->window[s->strstart-1]));
; 1650 :             _tr_tally_lit(s, s->window[s->strstart-1], bflush);

	mov	ecx, DWORD PTR [esi+56]
	xor	ebx, ebx
	mov	eax, DWORD PTR [esi+108]
	mov	dl, BYTE PTR [ecx+eax-1]
	mov	ecx, DWORD PTR [esi+5792]
	mov	eax, DWORD PTR [esi+5796]
	mov	WORD PTR [eax+ecx*2], bx
	mov	eax, DWORD PTR [esi+5792]
	mov	ecx, DWORD PTR [esi+5784]
	mov	BYTE PTR [ecx+eax], dl
	inc	DWORD PTR [esi+5792]
	movzx	eax, dl
	inc	WORD PTR [esi+eax*4+148]
	mov	eax, DWORD PTR [esi+5788]
	dec	eax
	cmp	DWORD PTR [esi+5792], eax

; 1651 :             if (bflush) {

	jne	SHORT $LN46@deflate_sl

; 1652 :                 FLUSH_BLOCK_ONLY(s, 0);

	mov	edx, DWORD PTR [esi+92]
	test	edx, edx
	js	SHORT $LN34@deflate_sl
	mov	ecx, DWORD PTR [esi+56]
	add	ecx, edx
	jmp	SHORT $LN35@deflate_sl
$LN34@deflate_sl:
	xor	ecx, ecx
$LN35@deflate_sl:
	mov	eax, DWORD PTR [esi+108]
	push	0
	sub	eax, edx
	push	eax
	push	ecx
	push	esi
	call	__tr_flush_block
	mov	edi, DWORD PTR [esi]
	add	esp, 16					; 00000010H
	mov	eax, DWORD PTR [esi+108]
	mov	DWORD PTR [esi+92], eax

; 535  :     unsigned len = strm->state->pending;

	mov	ecx, DWORD PTR [edi+28]

; 536  : 
; 537  :     if (len > strm->avail_out) len = strm->avail_out;

	mov	ebx, DWORD PTR [edi+16]

; 538  :     if (len == 0) return;

	cmp	DWORD PTR [ecx+20], ebx
	cmovbe	ebx, DWORD PTR [ecx+20]
	test	ebx, ebx
	je	SHORT $LN69@deflate_sl

; 539  : 
; 540  :     zmemcpy(strm->next_out, strm->state->pending_out, len);

	push	ebx
	push	DWORD PTR [ecx+16]
	push	DWORD PTR [edi+12]
	call	_memcpy

; 541  :     strm->next_out  += len;
; 542  :     strm->state->pending_out  += len;

	mov	eax, DWORD PTR [edi+28]
	add	esp, 12					; 0000000cH
	add	DWORD PTR [edi+12], ebx
	add	DWORD PTR [eax+16], ebx

; 543  :     strm->total_out += len;
; 544  :     strm->avail_out  -= len;
; 545  :     strm->state->pending -= len;

	mov	eax, DWORD PTR [edi+28]
	add	DWORD PTR [edi+20], ebx
	sub	DWORD PTR [edi+16], ebx
	sub	DWORD PTR [eax+20], ebx

; 546  :     if (strm->state->pending == 0) {

	mov	ecx, DWORD PTR [edi+28]
	mov	edi, DWORD PTR _hash_head$1$[esp+24]
	cmp	DWORD PTR [ecx+20], 0
	jne	SHORT $LN46@deflate_sl

; 547  :         strm->state->pending_out = strm->state->pending_buf;

	mov	eax, DWORD PTR [ecx+8]
	mov	DWORD PTR [ecx+16], eax
	jmp	SHORT $LN46@deflate_sl
$LN69@deflate_sl:

; 538  :     if (len == 0) return;

	mov	edi, DWORD PTR _hash_head$1$[esp+24]
$LN46@deflate_sl:

; 1653 :             }
; 1654 :             s->strstart++;
; 1655 :             s->lookahead--;
; 1656 :             if (s->strm->avail_out == 0) return need_more;

	mov	eax, DWORD PTR [esi]
	inc	DWORD PTR [esi+108]
	dec	DWORD PTR [esi+116]
	cmp	DWORD PTR [eax+16], 0
	jne	$LL2@deflate_sl
$LN59@deflate_sl:
	pop	edi

; 1674 : }

	pop	esi
	pop	ebp
	xor	eax, eax
	pop	ebx
	add	esp, 8
	ret	0
$LN23@deflate_sl:

; 1657 :         } else {
; 1658 :             /* There is no previous match to compare with, wait for
; 1659 :              * the next step to decide.
; 1660 :              */
; 1661 :             s->match_available = 1;
; 1662 :             s->strstart++;

	inc	DWORD PTR [esi+108]

; 1663 :             s->lookahead--;

	dec	DWORD PTR [esi+116]
	mov	DWORD PTR [esi+104], 1

; 1664 :         }
; 1665 :     }

	jmp	$LL2@deflate_sl
$LN57@deflate_sl:

; 1666 :     Assert (flush != Z_NO_FLUSH, "no flush?");
; 1667 :     if (s->match_available) {

	cmp	DWORD PTR [esi+104], 0
	je	SHORT $LN27@deflate_sl

; 1668 :         Tracevv((stderr,"%c", s->window[s->strstart-1]));
; 1669 :         _tr_tally_lit(s, s->window[s->strstart-1], bflush);

	mov	ecx, DWORD PTR [esi+108]
	xor	edi, edi
	mov	eax, DWORD PTR [esi+56]
	mov	dl, BYTE PTR [ecx+eax-1]
	mov	ecx, DWORD PTR [esi+5792]
	mov	eax, DWORD PTR [esi+5796]
	mov	WORD PTR [eax+ecx*2], di
	mov	eax, DWORD PTR [esi+5792]
	mov	ecx, DWORD PTR [esi+5784]
	mov	BYTE PTR [ecx+eax], dl
	inc	DWORD PTR [esi+5792]
	movzx	eax, dl
	inc	WORD PTR [esi+eax*4+148]

; 1670 :         s->match_available = 0;

	mov	DWORD PTR [esi+104], edi
$LN27@deflate_sl:

; 1671 :     }
; 1672 :     FLUSH_BLOCK(s, flush == Z_FINISH);

	mov	edx, DWORD PTR [esi+92]
	test	edx, edx
	js	SHORT $LN36@deflate_sl
	mov	ecx, DWORD PTR [esi+56]
	add	ecx, edx
	jmp	SHORT $LN37@deflate_sl
$LN36@deflate_sl:
	xor	ecx, ecx
$LN37@deflate_sl:
	xor	eax, eax
	cmp	ebp, 4
	sete	al
	push	eax
	mov	eax, DWORD PTR [esi+108]
	sub	eax, edx
	push	eax
	push	ecx
	push	esi
	call	__tr_flush_block
	mov	edi, DWORD PTR [esi]
	add	esp, 16					; 00000010H
	mov	eax, DWORD PTR [esi+108]
	mov	DWORD PTR [esi+92], eax

; 535  :     unsigned len = strm->state->pending;

	mov	ecx, DWORD PTR [edi+28]

; 536  : 
; 537  :     if (len > strm->avail_out) len = strm->avail_out;

	mov	ebx, DWORD PTR [edi+16]

; 538  :     if (len == 0) return;

	cmp	DWORD PTR [ecx+20], ebx
	cmovbe	ebx, DWORD PTR [ecx+20]
	test	ebx, ebx
	je	SHORT $LN51@deflate_sl

; 539  : 
; 540  :     zmemcpy(strm->next_out, strm->state->pending_out, len);

	push	ebx
	push	DWORD PTR [ecx+16]
	push	DWORD PTR [edi+12]
	call	_memcpy

; 541  :     strm->next_out  += len;
; 542  :     strm->state->pending_out  += len;

	mov	eax, DWORD PTR [edi+28]
	add	esp, 12					; 0000000cH
	add	DWORD PTR [edi+12], ebx
	add	DWORD PTR [eax+16], ebx

; 543  :     strm->total_out += len;
; 544  :     strm->avail_out  -= len;
; 545  :     strm->state->pending -= len;

	mov	eax, DWORD PTR [edi+28]
	add	DWORD PTR [edi+20], ebx
	sub	DWORD PTR [edi+16], ebx
	sub	DWORD PTR [eax+20], ebx

; 546  :     if (strm->state->pending == 0) {

	mov	ecx, DWORD PTR [edi+28]
	cmp	DWORD PTR [ecx+20], 0
	jne	SHORT $LN51@deflate_sl

; 547  :         strm->state->pending_out = strm->state->pending_buf;

	mov	eax, DWORD PTR [ecx+8]
	mov	DWORD PTR [ecx+16], eax
$LN51@deflate_sl:

; 1671 :     }
; 1672 :     FLUSH_BLOCK(s, flush == Z_FINISH);

	mov	eax, DWORD PTR [esi]
	cmp	DWORD PTR [eax+16], 0
	jne	SHORT $LN28@deflate_sl
	pop	edi
	xor	eax, eax
	mov	ecx, 2

; 1674 : }

	pop	esi
	cmp	ebp, 4
	pop	ebp
	cmove	eax, ecx
	pop	ebx
	add	esp, 8
	ret	0
$LN28@deflate_sl:

; 1673 :     return flush == Z_FINISH ? finish_done : block_done;

	xor	eax, eax
	cmp	ebp, 4
	pop	edi
	sete	al

; 1674 : }

	pop	esi
	pop	ebp
	pop	ebx
	lea	eax, DWORD PTR [eax*2+1]
	add	esp, 8
	ret	0
_deflate_slow ENDP
_TEXT	ENDS
; Function compile flags: /Ogtpy
; File d:\repo\msx-master\msx\bluemsx-dpi\src\unzip\deflate.c
;	COMDAT _deflate_fast
_TEXT	SEGMENT
_s$ = 8							; size = 4
_flush$ = 12						; size = 4
_deflate_fast PROC					; COMDAT

; 1451 : {

	push	ebx
	push	ebp
	push	esi

; 1452 :     IPos hash_head = NIL; /* head of the hash chain */

	mov	esi, DWORD PTR _s$[esp+8]
	xor	ebp, ebp
	push	edi
$LN54@deflate_fa:

; 1453 :     int bflush;           /* set if current block must be flushed */
; 1454 : 
; 1455 :     for (;;) {
; 1456 :         /* Make sure that we always have enough lookahead, except
; 1457 :          * at the end of the input file. We need MAX_MATCH bytes
; 1458 :          * for the next match, plus MIN_MATCH bytes to insert the
; 1459 :          * string following the next match.
; 1460 :          */
; 1461 :         if (s->lookahead < MIN_LOOKAHEAD) {

	mov	ebx, DWORD PTR _flush$[esp+12]
	npad	2
$LL2@deflate_fa:
	mov	eax, DWORD PTR [esi+116]
	cmp	eax, 262				; 00000106H
	jae	SHORT $LN10@deflate_fa

; 1462 :             fill_window(s);

	push	esi
	call	_fill_window

; 1463 :             if (s->lookahead < MIN_LOOKAHEAD && flush == Z_NO_FLUSH) {

	mov	eax, DWORD PTR [esi+116]
	add	esp, 4
	cmp	eax, 262				; 00000106H
	jae	SHORT $LN9@deflate_fa
	test	ebx, ebx
	je	$LN45@deflate_fa
$LN9@deflate_fa:

; 1464 :                 return need_more;
; 1465 :             }
; 1466 :             if (s->lookahead == 0) break; /* flush the current block */

	test	eax, eax
	je	$LN44@deflate_fa
$LN10@deflate_fa:

; 1467 :         }
; 1468 : 
; 1469 :         /* Insert the string window[strstart .. strstart+2] in the
; 1470 :          * dictionary, and set hash_head to the head of the hash chain:
; 1471 :          */
; 1472 :         if (s->lookahead >= MIN_MATCH) {

	cmp	eax, 3
	jb	SHORT $LN11@deflate_fa

; 1473 :             INSERT_STRING(s, s->strstart, hash_head);

	mov	edi, DWORD PTR [esi+108]
	mov	eax, DWORD PTR [esi+56]
	mov	edx, DWORD PTR [esi+72]
	mov	ecx, DWORD PTR [esi+88]
	shl	edx, cl
	movzx	eax, BYTE PTR [eax+edi+2]
	and	edi, DWORD PTR [esi+52]
	xor	edx, eax
	and	edx, DWORD PTR [esi+84]
	mov	eax, DWORD PTR [esi+68]
	mov	ecx, DWORD PTR [esi+64]
	mov	DWORD PTR [esi+72], edx
	mov	ax, WORD PTR [eax+edx*2]
	mov	WORD PTR [ecx+edi*2], ax
	mov	edx, DWORD PTR [esi+108]
	mov	ecx, edx
	and	ecx, DWORD PTR [esi+52]
	mov	eax, DWORD PTR [esi+64]
	movzx	ebp, WORD PTR [eax+ecx*2]
	mov	ecx, DWORD PTR [esi+72]
	mov	eax, DWORD PTR [esi+68]
	mov	WORD PTR [eax+ecx*2], dx
$LN11@deflate_fa:

; 1474 :         }
; 1475 : 
; 1476 :         /* Find the longest match, discarding those <= prev_length.
; 1477 :          * At this point we have always match_length < MIN_MATCH
; 1478 :          */
; 1479 :         if (hash_head != NIL && s->strstart - hash_head <= MAX_DIST(s)) {

	test	ebp, ebp
	je	SHORT $LN15@deflate_fa
	mov	ecx, DWORD PTR [esi+108]
	mov	eax, DWORD PTR [esi+44]
	sub	ecx, ebp
	sub	eax, 262				; 00000106H
	cmp	ecx, eax
	ja	SHORT $LN15@deflate_fa

; 1480 :             /* To simplify the code, we prevent matches with the string
; 1481 :              * of window index 0 (in particular we have to avoid a match
; 1482 :              * of the string with itself at the start of the input file).
; 1483 :              */
; 1484 : #ifdef FASTEST
; 1485 :             if ((s->strategy != Z_HUFFMAN_ONLY && s->strategy != Z_RLE) ||
; 1486 :                 (s->strategy == Z_RLE && s->strstart - hash_head == 1)) {
; 1487 :                 s->match_length = longest_match_fast (s, hash_head);
; 1488 :             }
; 1489 : #else
; 1490 :             if (s->strategy != Z_HUFFMAN_ONLY && s->strategy != Z_RLE) {

	mov	eax, DWORD PTR [esi+136]
	cmp	eax, 2
	je	SHORT $LN15@deflate_fa
	cmp	eax, 3
	je	SHORT $LN51@deflate_fa

; 1491 :                 s->match_length = longest_match (s, hash_head);

	push	ebp
	push	esi
	call	_longest_match
	jmp	SHORT $LN52@deflate_fa
$LN51@deflate_fa:

; 1492 :             } else if (s->strategy == Z_RLE && s->strstart - hash_head == 1) {

	cmp	ecx, 1
	jne	SHORT $LN15@deflate_fa

; 1493 :                 s->match_length = longest_match_fast (s, hash_head);

	push	ebp
	push	esi
	call	_longest_match_fast
$LN52@deflate_fa:

; 1494 :             }
; 1495 : #endif
; 1496 :             /* longest_match() or longest_match_fast() sets match_start */
; 1497 :         }
; 1498 :         if (s->match_length >= MIN_MATCH) {

	mov	DWORD PTR [esi+96], eax
	add	esp, 8
$LN15@deflate_fa:
	mov	ebx, DWORD PTR [esi+96]
	cmp	ebx, 3
	jb	$LN16@deflate_fa

; 1499 :             check_match(s, s->strstart, s->match_start, s->match_length);
; 1500 : 
; 1501 :             _tr_tally_dist(s, s->strstart - s->match_start,

	mov	ax, WORD PTR [esi+108]
	sub	bl, 3
	sub	ax, WORD PTR [esi+112]
	mov	ecx, DWORD PTR [esi+5792]
	movzx	edx, ax
	mov	eax, DWORD PTR [esi+5796]
	mov	WORD PTR [eax+ecx*2], dx
	add	edx, 65535				; 0000ffffH
	mov	ecx, DWORD PTR [esi+5792]
	mov	eax, DWORD PTR [esi+5784]
	mov	BYTE PTR [ecx+eax], bl
	mov	ecx, 256				; 00000100H
	inc	DWORD PTR [esi+5792]
	movzx	eax, bl
	movzx	eax, BYTE PTR __length_code[eax]
	inc	WORD PTR [esi+eax*4+1176]
	movzx	eax, dx
	cmp	dx, cx
	jae	SHORT $LN24@deflate_fa
	mov	al, BYTE PTR __dist_code[eax]
	jmp	SHORT $LN25@deflate_fa
$LN24@deflate_fa:
	shr	eax, 7
	mov	al, BYTE PTR __dist_code[eax+256]
$LN25@deflate_fa:
	movzx	eax, al
	xor	ebx, ebx
	inc	WORD PTR [esi+eax*4+2440]
	mov	eax, DWORD PTR [esi+5788]

; 1502 :                            s->match_length - MIN_MATCH, bflush);
; 1503 : 
; 1504 :             s->lookahead -= s->match_length;

	mov	ecx, DWORD PTR [esi+96]
	dec	eax
	cmp	DWORD PTR [esi+5792], eax
	mov	eax, DWORD PTR [esi+116]
	sete	bl
	sub	eax, ecx
	mov	DWORD PTR [esi+116], eax

; 1505 : 
; 1506 :             /* Insert new strings in the hash table only if the match length
; 1507 :              * is not too large. This saves time but degrades compression.
; 1508 :              */
; 1509 : #ifndef FASTEST
; 1510 :             if (s->match_length <= s->max_insert_length &&

	cmp	ecx, DWORD PTR [esi+128]
	ja	SHORT $LN18@deflate_fa
	cmp	eax, 3
	jb	SHORT $LN18@deflate_fa

; 1511 :                 s->lookahead >= MIN_MATCH) {
; 1512 :                 s->match_length--; /* string at strstart already in table */

	lea	eax, DWORD PTR [ecx-1]
	mov	DWORD PTR [esi+96], eax
$LL7@deflate_fa:

; 1513 :                 do {
; 1514 :                     s->strstart++;

	inc	DWORD PTR [esi+108]
	mov	edi, DWORD PTR [esi+108]

; 1515 :                     INSERT_STRING(s, s->strstart, hash_head);

	mov	eax, DWORD PTR [esi+56]
	mov	ecx, DWORD PTR [esi+88]
	movzx	edx, BYTE PTR [edi+eax+2]
	mov	eax, DWORD PTR [esi+72]
	and	edi, DWORD PTR [esi+52]
	shl	eax, cl
	mov	ecx, DWORD PTR [esi+64]
	xor	edx, eax
	and	edx, DWORD PTR [esi+84]
	mov	eax, DWORD PTR [esi+68]
	mov	DWORD PTR [esi+72], edx
	mov	ax, WORD PTR [eax+edx*2]
	mov	WORD PTR [ecx+edi*2], ax
	mov	edx, DWORD PTR [esi+108]
	mov	ecx, edx
	and	ecx, DWORD PTR [esi+52]
	mov	eax, DWORD PTR [esi+64]
	movzx	ebp, WORD PTR [eax+ecx*2]
	mov	ecx, DWORD PTR [esi+72]
	mov	eax, DWORD PTR [esi+68]
	mov	WORD PTR [eax+ecx*2], dx

; 1516 :                     /* strstart never exceeds WSIZE-MAX_MATCH, so there are
; 1517 :                      * always MIN_MATCH bytes ahead.
; 1518 :                      */
; 1519 :                 } while (--s->match_length != 0);

	add	DWORD PTR [esi+96], -1
	jne	SHORT $LL7@deflate_fa

; 1520 :                 s->strstart++;
; 1521 :             } else

	jmp	$LN53@deflate_fa
$LN18@deflate_fa:

; 1522 : #endif
; 1523 :             {
; 1524 :                 s->strstart += s->match_length;

	mov	edi, DWORD PTR [esi+108]

; 1525 :                 s->match_length = 0;
; 1526 :                 s->ins_h = s->window[s->strstart];

	mov	eax, DWORD PTR [esi+56]
	add	edi, ecx

; 1527 :                 UPDATE_HASH(s, s->ins_h, s->window[s->strstart+1]);

	mov	ecx, DWORD PTR [esi+88]
	mov	DWORD PTR [esi+108], edi
	mov	DWORD PTR [esi+96], 0
	movzx	edx, BYTE PTR [eax+edi]
	mov	DWORD PTR [esi+72], edx
	movzx	eax, BYTE PTR [eax+edi+1]
	shl	edx, cl
	xor	edx, eax
	and	edx, DWORD PTR [esi+84]
	mov	DWORD PTR [esi+72], edx

; 1528 : #if MIN_MATCH != 3
; 1529 :                 Call UPDATE_HASH() MIN_MATCH-3 more times
; 1530 : #endif
; 1531 :                 /* If lookahead < MIN_MATCH, ins_h is garbage, but it does not
; 1532 :                  * matter since it will be recomputed at next deflate call.
; 1533 :                  */
; 1534 :             }
; 1535 :         } else {

	jmp	SHORT $LN17@deflate_fa
$LN16@deflate_fa:

; 1536 :             /* No match, output a literal byte */
; 1537 :             Tracevv((stderr,"%c", s->window[s->strstart]));
; 1538 :             _tr_tally_lit (s, s->window[s->strstart], bflush);

	mov	ecx, DWORD PTR [esi+56]
	xor	edi, edi
	mov	eax, DWORD PTR [esi+108]
	xor	ebx, ebx
	mov	dl, BYTE PTR [ecx+eax]
	mov	ecx, DWORD PTR [esi+5792]
	mov	eax, DWORD PTR [esi+5796]
	mov	WORD PTR [eax+ecx*2], di
	mov	ecx, DWORD PTR [esi+5792]
	mov	eax, DWORD PTR [esi+5784]
	mov	BYTE PTR [ecx+eax], dl
	inc	DWORD PTR [esi+5792]
	movzx	eax, dl
	inc	WORD PTR [esi+eax*4+148]
	mov	eax, DWORD PTR [esi+5788]
	dec	eax
	cmp	DWORD PTR [esi+5792], eax
	sete	bl

; 1539 :             s->lookahead--;

	dec	DWORD PTR [esi+116]
$LN53@deflate_fa:

; 1540 :             s->strstart++;
; 1541 :         }
; 1542 :         if (bflush) FLUSH_BLOCK(s, 0);

	inc	DWORD PTR [esi+108]
	mov	edi, DWORD PTR [esi+108]
$LN17@deflate_fa:
	test	ebx, ebx
	mov	ebx, DWORD PTR _flush$[esp+12]
	je	$LL2@deflate_fa
	mov	ecx, DWORD PTR [esi+92]
	test	ecx, ecx
	js	SHORT $LN26@deflate_fa
	mov	eax, DWORD PTR [esi+56]
	add	eax, ecx
	jmp	SHORT $LN27@deflate_fa
$LN26@deflate_fa:
	xor	eax, eax
$LN27@deflate_fa:
	push	0
	sub	edi, ecx
	push	edi
	push	eax
	push	esi
	call	__tr_flush_block
	mov	edi, DWORD PTR [esi]
	add	esp, 16					; 00000010H
	mov	eax, DWORD PTR [esi+108]
	mov	DWORD PTR [esi+92], eax

; 535  :     unsigned len = strm->state->pending;

	mov	ecx, DWORD PTR [edi+28]

; 536  : 
; 537  :     if (len > strm->avail_out) len = strm->avail_out;

	mov	ebx, DWORD PTR [edi+16]

; 538  :     if (len == 0) return;

	cmp	DWORD PTR [ecx+20], ebx
	cmovbe	ebx, DWORD PTR [ecx+20]
	test	ebx, ebx
	je	SHORT $LN33@deflate_fa

; 539  : 
; 540  :     zmemcpy(strm->next_out, strm->state->pending_out, len);

	push	ebx
	push	DWORD PTR [ecx+16]
	push	DWORD PTR [edi+12]
	call	_memcpy

; 541  :     strm->next_out  += len;
; 542  :     strm->state->pending_out  += len;

	mov	eax, DWORD PTR [edi+28]
	add	esp, 12					; 0000000cH
	add	DWORD PTR [edi+12], ebx
	add	DWORD PTR [eax+16], ebx

; 543  :     strm->total_out += len;
; 544  :     strm->avail_out  -= len;
; 545  :     strm->state->pending -= len;

	mov	eax, DWORD PTR [edi+28]
	add	DWORD PTR [edi+20], ebx
	sub	DWORD PTR [edi+16], ebx
	sub	DWORD PTR [eax+20], ebx

; 546  :     if (strm->state->pending == 0) {

	mov	ecx, DWORD PTR [edi+28]
	cmp	DWORD PTR [ecx+20], 0
	jne	SHORT $LN33@deflate_fa

; 547  :         strm->state->pending_out = strm->state->pending_buf;

	mov	eax, DWORD PTR [ecx+8]
	mov	DWORD PTR [ecx+16], eax
$LN33@deflate_fa:

; 1540 :             s->strstart++;
; 1541 :         }
; 1542 :         if (bflush) FLUSH_BLOCK(s, 0);

	mov	eax, DWORD PTR [esi]
	cmp	DWORD PTR [eax+16], 0
	jne	$LN54@deflate_fa
$LN45@deflate_fa:
	pop	edi

; 1546 : }

	pop	esi
	pop	ebp
	xor	eax, eax
	pop	ebx
	ret	0
$LN44@deflate_fa:

; 1543 :     }
; 1544 :     FLUSH_BLOCK(s, flush == Z_FINISH);

	mov	edx, DWORD PTR [esi+92]
	test	edx, edx
	js	SHORT $LN28@deflate_fa
	mov	ecx, DWORD PTR [esi+56]
	add	ecx, edx
	jmp	SHORT $LN29@deflate_fa
$LN28@deflate_fa:
	xor	ecx, ecx
$LN29@deflate_fa:
	xor	eax, eax
	cmp	ebx, 4
	sete	al
	push	eax
	mov	eax, DWORD PTR [esi+108]
	sub	eax, edx
	push	eax
	push	ecx
	push	esi
	call	__tr_flush_block
	mov	edi, DWORD PTR [esi]
	add	esp, 16					; 00000010H
	mov	eax, DWORD PTR [esi+108]
	mov	DWORD PTR [esi+92], eax

; 535  :     unsigned len = strm->state->pending;

	mov	ecx, DWORD PTR [edi+28]

; 536  : 
; 537  :     if (len > strm->avail_out) len = strm->avail_out;

	mov	ebx, DWORD PTR [edi+16]

; 538  :     if (len == 0) return;

	cmp	DWORD PTR [ecx+20], ebx
	cmovbe	ebx, DWORD PTR [ecx+20]
	test	ebx, ebx
	je	SHORT $LN38@deflate_fa

; 539  : 
; 540  :     zmemcpy(strm->next_out, strm->state->pending_out, len);

	push	ebx
	push	DWORD PTR [ecx+16]
	push	DWORD PTR [edi+12]
	call	_memcpy

; 541  :     strm->next_out  += len;
; 542  :     strm->state->pending_out  += len;

	mov	eax, DWORD PTR [edi+28]
	add	esp, 12					; 0000000cH
	add	DWORD PTR [edi+12], ebx
	add	DWORD PTR [eax+16], ebx

; 543  :     strm->total_out += len;
; 544  :     strm->avail_out  -= len;
; 545  :     strm->state->pending -= len;

	mov	eax, DWORD PTR [edi+28]
	add	DWORD PTR [edi+20], ebx
	sub	DWORD PTR [edi+16], ebx
	sub	DWORD PTR [eax+20], ebx

; 546  :     if (strm->state->pending == 0) {

	mov	ecx, DWORD PTR [edi+28]
	cmp	DWORD PTR [ecx+20], 0
	jne	SHORT $LN38@deflate_fa

; 547  :         strm->state->pending_out = strm->state->pending_buf;

	mov	eax, DWORD PTR [ecx+8]
	mov	DWORD PTR [ecx+16], eax
$LN38@deflate_fa:

; 1543 :     }
; 1544 :     FLUSH_BLOCK(s, flush == Z_FINISH);

	mov	eax, DWORD PTR [esi]
	cmp	DWORD PTR [eax+16], 0
	jne	SHORT $LN22@deflate_fa
	pop	edi
	xor	eax, eax
	mov	edx, 2
	cmp	DWORD PTR _flush$[esp+8], 4

; 1546 : }

	pop	esi
	pop	ebp
	cmove	eax, edx
	pop	ebx
	ret	0
$LN22@deflate_fa:

; 1545 :     return flush == Z_FINISH ? finish_done : block_done;

	xor	eax, eax
	cmp	DWORD PTR _flush$[esp+12], 4
	pop	edi
	sete	al

; 1546 : }

	pop	esi
	pop	ebp
	pop	ebx
	lea	eax, DWORD PTR [eax*2+1]
	ret	0
_deflate_fast ENDP
_TEXT	ENDS
; Function compile flags: /Ogtpy
; File d:\repo\msx-master\msx\bluemsx-dpi\src\unzip\deflate.c
;	COMDAT _deflate_stored
_TEXT	SEGMENT
_s$ = 8							; size = 4
_flush$ = 12						; size = 4
_deflate_stored PROC					; COMDAT

; 1393 : {

	push	ebx
	push	ebp
	push	esi

; 1394 :     /* Stored blocks are limited to 0xffff bytes, pending_buf is limited
; 1395 :      * to pending_buf_size, and each stored block has a 5 byte header:
; 1396 :      */
; 1397 :     ulg max_block_size = 0xffff;
; 1398 :     ulg max_start;
; 1399 : 
; 1400 :     if (max_block_size > s->pending_buf_size - 5) {

	mov	esi, DWORD PTR _s$[esp+8]
	mov	ebp, 65535				; 0000ffffH
	push	edi
	mov	eax, DWORD PTR [esi+12]
	add	eax, -5					; fffffffbH
	cmp	eax, ebp
	cmovb	ebp, eax
$LN53@deflate_st:

; 1401 :         max_block_size = s->pending_buf_size - 5;
; 1402 :     }
; 1403 : 
; 1404 :     /* Copy as much as possible from input to output: */
; 1405 :     for (;;) {
; 1406 :         /* Fill the window as much as possible: */
; 1407 :         if (s->lookahead <= 1) {

	mov	ebx, DWORD PTR _flush$[esp+12]
	npad	4
$LL2@deflate_st:
	mov	eax, DWORD PTR [esi+116]
	cmp	eax, 1
	ja	SHORT $LN8@deflate_st

; 1408 : 
; 1409 :             Assert(s->strstart < s->w_size+MAX_DIST(s) ||
; 1410 :                    s->block_start >= (long)s->w_size, "slide too late");
; 1411 : 
; 1412 :             fill_window(s);

	push	esi
	call	_fill_window

; 1413 :             if (s->lookahead == 0 && flush == Z_NO_FLUSH) return need_more;

	mov	eax, DWORD PTR [esi+116]
	add	esp, 4
	test	eax, eax
	jne	SHORT $LN8@deflate_st
	test	ebx, ebx
	je	$LN42@deflate_st

; 1414 : 
; 1415 :             if (s->lookahead == 0) break; /* flush the current block */

	test	eax, eax
	je	$LN40@deflate_st
$LN8@deflate_st:

; 1416 :         }
; 1417 :         Assert(s->block_start >= 0L, "block gone");
; 1418 : 
; 1419 :         s->strstart += s->lookahead;
; 1420 :         s->lookahead = 0;
; 1421 : 
; 1422 :         /* Emit a stored block if pending_buf will be full: */
; 1423 :         max_start = s->block_start + max_block_size;

	mov	edx, DWORD PTR [esi+92]
	add	DWORD PTR [esi+108], eax
	mov	eax, DWORD PTR [esi+108]
	mov	DWORD PTR [esi+116], 0
	lea	ecx, DWORD PTR [edx+ebp]

; 1424 :         if (s->strstart == 0 || (ulg)s->strstart >= max_start) {

	je	SHORT $LN46@deflate_st
	cmp	eax, ecx
	jb	SHORT $LN50@deflate_st
$LN46@deflate_st:

; 1425 :             /* strstart == 0 is possible when wraparound on 16-bit machine */
; 1426 :             s->lookahead = (uInt)(s->strstart - max_start);

	sub	eax, ecx

; 1427 :             s->strstart = (uInt)max_start;

	mov	DWORD PTR [esi+108], ecx
	mov	DWORD PTR [esi+116], eax

; 1428 :             FLUSH_BLOCK(s, 0);

	test	edx, edx
	js	SHORT $LN16@deflate_st
	mov	eax, DWORD PTR [esi+56]
	add	eax, edx
	jmp	SHORT $LN17@deflate_st
$LN16@deflate_st:
	xor	eax, eax
$LN17@deflate_st:
	push	0
	push	ebp
	push	eax
	push	esi
	call	__tr_flush_block
	mov	edi, DWORD PTR [esi]
	add	esp, 16					; 00000010H
	mov	eax, DWORD PTR [esi+108]
	mov	DWORD PTR [esi+92], eax

; 535  :     unsigned len = strm->state->pending;

	mov	ecx, DWORD PTR [edi+28]

; 536  : 
; 537  :     if (len > strm->avail_out) len = strm->avail_out;

	mov	ebx, DWORD PTR [edi+16]

; 538  :     if (len == 0) return;

	cmp	DWORD PTR [ecx+20], ebx
	cmovbe	ebx, DWORD PTR [ecx+20]
	test	ebx, ebx
	je	SHORT $LN25@deflate_st

; 539  : 
; 540  :     zmemcpy(strm->next_out, strm->state->pending_out, len);

	push	ebx
	push	DWORD PTR [ecx+16]
	push	DWORD PTR [edi+12]
	call	_memcpy

; 541  :     strm->next_out  += len;
; 542  :     strm->state->pending_out  += len;

	mov	eax, DWORD PTR [edi+28]
	add	esp, 12					; 0000000cH
	add	DWORD PTR [edi+12], ebx
	add	DWORD PTR [eax+16], ebx

; 543  :     strm->total_out += len;
; 544  :     strm->avail_out  -= len;
; 545  :     strm->state->pending -= len;

	mov	eax, DWORD PTR [edi+28]
	add	DWORD PTR [edi+20], ebx
	sub	DWORD PTR [edi+16], ebx
	sub	DWORD PTR [eax+20], ebx

; 546  :     if (strm->state->pending == 0) {

	mov	ecx, DWORD PTR [edi+28]
	cmp	DWORD PTR [ecx+20], 0
	jne	SHORT $LN25@deflate_st

; 547  :         strm->state->pending_out = strm->state->pending_buf;

	mov	eax, DWORD PTR [ecx+8]
	mov	DWORD PTR [ecx+16], eax
$LN25@deflate_st:

; 1428 :             FLUSH_BLOCK(s, 0);

	mov	eax, DWORD PTR [esi]
	cmp	DWORD PTR [eax+16], 0
	je	$LN42@deflate_st
	mov	ebx, DWORD PTR _flush$[esp+12]
$LN50@deflate_st:

; 1429 :         }
; 1430 :         /* Flush if we may have to slide, otherwise block_start may become
; 1431 :          * negative and the data will be gone:
; 1432 :          */
; 1433 :         if (s->strstart - (uInt)s->block_start >= MAX_DIST(s)) {

	mov	ecx, DWORD PTR [esi+108]
	mov	eax, DWORD PTR [esi+44]
	mov	edx, DWORD PTR [esi+92]
	sub	eax, 262				; 00000106H
	sub	ecx, edx
	cmp	ecx, eax
	jb	$LL2@deflate_st

; 1434 :             FLUSH_BLOCK(s, 0);

	test	edx, edx
	js	SHORT $LN18@deflate_st
	mov	eax, DWORD PTR [esi+56]
	add	eax, edx
	jmp	SHORT $LN19@deflate_st
$LN18@deflate_st:
	xor	eax, eax
$LN19@deflate_st:
	push	0
	push	ecx
	push	eax
	push	esi
	call	__tr_flush_block
	mov	edi, DWORD PTR [esi]
	add	esp, 16					; 00000010H
	mov	eax, DWORD PTR [esi+108]
	mov	DWORD PTR [esi+92], eax

; 535  :     unsigned len = strm->state->pending;

	mov	ecx, DWORD PTR [edi+28]

; 536  : 
; 537  :     if (len > strm->avail_out) len = strm->avail_out;

	mov	ebx, DWORD PTR [edi+16]

; 538  :     if (len == 0) return;

	cmp	DWORD PTR [ecx+20], ebx
	cmovbe	ebx, DWORD PTR [ecx+20]
	test	ebx, ebx
	je	SHORT $LN30@deflate_st

; 539  : 
; 540  :     zmemcpy(strm->next_out, strm->state->pending_out, len);

	push	ebx
	push	DWORD PTR [ecx+16]
	push	DWORD PTR [edi+12]
	call	_memcpy

; 541  :     strm->next_out  += len;
; 542  :     strm->state->pending_out  += len;

	mov	eax, DWORD PTR [edi+28]
	add	esp, 12					; 0000000cH
	add	DWORD PTR [edi+12], ebx
	add	DWORD PTR [eax+16], ebx

; 543  :     strm->total_out += len;
; 544  :     strm->avail_out  -= len;
; 545  :     strm->state->pending -= len;

	mov	eax, DWORD PTR [edi+28]
	add	DWORD PTR [edi+20], ebx
	sub	DWORD PTR [edi+16], ebx
	sub	DWORD PTR [eax+20], ebx

; 546  :     if (strm->state->pending == 0) {

	mov	ecx, DWORD PTR [edi+28]
	cmp	DWORD PTR [ecx+20], 0
	jne	SHORT $LN30@deflate_st

; 547  :         strm->state->pending_out = strm->state->pending_buf;

	mov	eax, DWORD PTR [ecx+8]
	mov	DWORD PTR [ecx+16], eax
$LN30@deflate_st:

; 1434 :             FLUSH_BLOCK(s, 0);

	mov	eax, DWORD PTR [esi]
	cmp	DWORD PTR [eax+16], 0
	jne	$LN53@deflate_st
$LN42@deflate_st:
	pop	edi

; 1439 : }

	pop	esi
	pop	ebp
	xor	eax, eax
	pop	ebx
	ret	0
$LN40@deflate_st:

; 1435 :         }
; 1436 :     }
; 1437 :     FLUSH_BLOCK(s, flush == Z_FINISH);

	mov	edx, DWORD PTR [esi+92]
	test	edx, edx
	js	SHORT $LN20@deflate_st
	mov	ecx, DWORD PTR [esi+56]
	add	ecx, edx
	jmp	SHORT $LN21@deflate_st
$LN20@deflate_st:
	xor	ecx, ecx
$LN21@deflate_st:
	xor	eax, eax
	cmp	ebx, 4
	sete	al
	push	eax
	mov	eax, DWORD PTR [esi+108]
	sub	eax, edx
	push	eax
	push	ecx
	push	esi
	call	__tr_flush_block
	mov	edi, DWORD PTR [esi]
	add	esp, 16					; 00000010H
	mov	eax, DWORD PTR [esi+108]
	mov	DWORD PTR [esi+92], eax

; 535  :     unsigned len = strm->state->pending;

	mov	ecx, DWORD PTR [edi+28]

; 536  : 
; 537  :     if (len > strm->avail_out) len = strm->avail_out;

	mov	ebx, DWORD PTR [edi+16]

; 538  :     if (len == 0) return;

	cmp	DWORD PTR [ecx+20], ebx
	cmovbe	ebx, DWORD PTR [ecx+20]
	test	ebx, ebx
	je	SHORT $LN35@deflate_st

; 539  : 
; 540  :     zmemcpy(strm->next_out, strm->state->pending_out, len);

	push	ebx
	push	DWORD PTR [ecx+16]
	push	DWORD PTR [edi+12]
	call	_memcpy

; 541  :     strm->next_out  += len;
; 542  :     strm->state->pending_out  += len;

	mov	eax, DWORD PTR [edi+28]
	add	esp, 12					; 0000000cH
	add	DWORD PTR [edi+12], ebx
	add	DWORD PTR [eax+16], ebx

; 543  :     strm->total_out += len;
; 544  :     strm->avail_out  -= len;
; 545  :     strm->state->pending -= len;

	mov	eax, DWORD PTR [edi+28]
	add	DWORD PTR [edi+20], ebx
	sub	DWORD PTR [edi+16], ebx
	sub	DWORD PTR [eax+20], ebx

; 546  :     if (strm->state->pending == 0) {

	mov	ecx, DWORD PTR [edi+28]
	cmp	DWORD PTR [ecx+20], 0
	jne	SHORT $LN35@deflate_st

; 547  :         strm->state->pending_out = strm->state->pending_buf;

	mov	eax, DWORD PTR [ecx+8]
	mov	DWORD PTR [ecx+16], eax
$LN35@deflate_st:

; 1435 :         }
; 1436 :     }
; 1437 :     FLUSH_BLOCK(s, flush == Z_FINISH);

	mov	eax, DWORD PTR [esi]
	cmp	DWORD PTR [eax+16], 0
	jne	SHORT $LN14@deflate_st
	pop	edi
	xor	eax, eax
	mov	edx, 2
	cmp	DWORD PTR _flush$[esp+8], 4

; 1439 : }

	pop	esi
	pop	ebp
	cmove	eax, edx
	pop	ebx
	ret	0
$LN14@deflate_st:

; 1438 :     return flush == Z_FINISH ? finish_done : block_done;

	xor	eax, eax
	cmp	DWORD PTR _flush$[esp+12], 4
	pop	edi
	sete	al

; 1439 : }

	pop	esi
	pop	ebp
	pop	ebx
	lea	eax, DWORD PTR [eax*2+1]
	ret	0
_deflate_stored ENDP
_TEXT	ENDS
; Function compile flags: /Ogtpy
; File d:\repo\msx-master\msx\bluemsx-dpi\src\unzip\deflate.c
;	COMDAT _fill_window
_TEXT	SEGMENT
tv636 = 8						; size = 4
tv628 = 8						; size = 4
_buf$1$ = 8						; size = 4
_s$ = 8							; size = 4
_fill_window PROC					; COMDAT

; 1268 : {

	push	ebx
	push	ebp
	push	esi
	push	edi

; 1269 :     register unsigned n, m;
; 1270 :     register Posf *p;
; 1271 :     unsigned more;    /* Amount of free space at the end of the window. */
; 1272 :     uInt wsize = s->w_size;

	mov	edi, DWORD PTR _s$[esp+12]
	mov	esi, DWORD PTR [edi+44]
	mov	ebp, DWORD PTR [edi+116]
	npad	2
$LL4@fill_windo:

; 1273 : 
; 1274 :     do {
; 1275 :         more = (unsigned)(s->window_size -(ulg)s->lookahead -(ulg)s->strstart);
; 1276 : 
; 1277 :         /* Deal with !@#$% 64K limit: */
; 1278 :         if (sizeof(int) <= 2) {
; 1279 :             if (more == 0 && s->strstart == 0 && s->lookahead == 0) {
; 1280 :                 more = wsize;
; 1281 : 
; 1282 :             } else if (more == (unsigned)(-1)) {
; 1283 :                 /* Very unlikely, but possible on 16 bit machine if
; 1284 :                  * strstart == 0 && lookahead == 1 (input done a byte at time)
; 1285 :                  */
; 1286 :                 more--;
; 1287 :             }
; 1288 :         }
; 1289 : 
; 1290 :         /* If the window is almost full and there is insufficient lookahead,
; 1291 :          * move the upper half to the lower one to make room in the upper half.
; 1292 :          */
; 1293 :         if (s->strstart >= wsize+MAX_DIST(s)) {

	mov	eax, DWORD PTR [edi+44]
	mov	ebx, DWORD PTR [edi+60]
	add	eax, -262				; fffffefaH
	sub	ebx, DWORD PTR [edi+108]
	add	eax, esi
	sub	ebx, ebp
	cmp	DWORD PTR [edi+108], eax
	jb	$LN15@fill_windo

; 1294 : 
; 1295 :             zmemcpy(s->window, s->window+wsize, (unsigned)wsize);

	mov	ecx, DWORD PTR [edi+56]
	push	esi
	lea	eax, DWORD PTR [ecx+esi]
	push	eax
	push	ecx
	call	_memcpy

; 1296 :             s->match_start -= wsize;
; 1297 :             s->strstart    -= wsize; /* we now have strstart >= MAX_DIST */
; 1298 :             s->block_start -= (long) wsize;
; 1299 : 
; 1300 :             /* Slide the hash table (could be avoided with 32 bit values
; 1301 :                at the expense of memory usage). We slide even when level == 0
; 1302 :                to keep the hash table consistent if we switch back to level > 0
; 1303 :                later. (Using level 0 permanently is not an optimal usage of
; 1304 :                zlib, so we don't care about this pathological case.)
; 1305 :              */
; 1306 :             /* %%% avoid this when Z_RLE */
; 1307 :             n = s->hash_size;

	mov	ebp, DWORD PTR [edi+76]
	add	esp, 12					; 0000000cH

; 1308 :             p = &s->head[n];

	mov	eax, DWORD PTR [edi+68]
	sub	DWORD PTR [edi+112], esi
	sub	DWORD PTR [edi+108], esi
	sub	DWORD PTR [edi+92], esi
	lea	edx, DWORD PTR [eax+ebp*2]
	npad	2
$LL7@fill_windo:

; 1309 :             do {
; 1310 :                 m = *--p;

	movzx	ecx, WORD PTR [edx-2]
	lea	edx, DWORD PTR [edx-2]

; 1311 :                 *p = (Pos)(m >= wsize ? m-wsize : NIL);

	mov	eax, ecx
	mov	DWORD PTR tv628[esp+12], 0
	sub	eax, esi
	cmp	ecx, esi
	movzx	eax, ax
	cmovb	eax, DWORD PTR tv628[esp+12]
	mov	WORD PTR [edx], ax

; 1312 :             } while (--n);

	sub	ebp, 1
	jne	SHORT $LL7@fill_windo

; 1313 : 
; 1314 :             n = wsize;
; 1315 : #ifndef FASTEST
; 1316 :             p = &s->prev[n];

	mov	eax, DWORD PTR [edi+64]
	mov	ebp, esi
	lea	edx, DWORD PTR [eax+esi*2]
	npad	3
$LL10@fill_windo:

; 1317 :             do {
; 1318 :                 m = *--p;

	movzx	ecx, WORD PTR [edx-2]
	lea	edx, DWORD PTR [edx-2]

; 1319 :                 *p = (Pos)(m >= wsize ? m-wsize : NIL);

	mov	eax, ecx
	mov	DWORD PTR tv636[esp+12], 0
	sub	eax, esi
	cmp	ecx, esi
	movzx	eax, ax
	cmovb	eax, DWORD PTR tv636[esp+12]
	mov	WORD PTR [edx], ax

; 1320 :                 /* If n is not on any hash chain, prev[n] is garbage but
; 1321 :                  * its value will never be used.
; 1322 :                  */
; 1323 :             } while (--n);

	sub	ebp, 1
	jne	SHORT $LL10@fill_windo

; 1324 : #endif
; 1325 :             more += wsize;

	add	ebx, esi
$LN15@fill_windo:

; 1326 :         }
; 1327 :         if (s->strm->avail_in == 0) return;

	mov	ebp, DWORD PTR [edi]
	mov	eax, DWORD PTR [ebp+4]
	test	eax, eax
	je	$LN3@fill_windo

; 1328 : 
; 1329 :         /* If there was no sliding:
; 1330 :          *    strstart <= WSIZE+MAX_DIST-1 && lookahead <= MIN_LOOKAHEAD - 1 &&
; 1331 :          *    more == window_size - lookahead - strstart
; 1332 :          * => more >= window_size - (MIN_LOOKAHEAD-1 + WSIZE + MAX_DIST-1)
; 1333 :          * => more >= window_size - 2*WSIZE + 2
; 1334 :          * In the BIG_MEM or MMAP case (not yet supported),
; 1335 :          *   window_size == input_size + MIN_LOOKAHEAD  &&
; 1336 :          *   strstart + s->lookahead <= input_size => more >= MIN_LOOKAHEAD.
; 1337 :          * Otherwise, window_size == 2*WSIZE so more >= 2.
; 1338 :          * If there was sliding, more >= WSIZE. So in all cases, more >= 2.
; 1339 :          */
; 1340 :         Assert(more >= 2, "more < 2");
; 1341 : 
; 1342 :         n = read_buf(s->strm, s->window + s->strstart + s->lookahead, more);

	mov	edx, DWORD PTR [edi+108]
	mov	ecx, DWORD PTR [edi+116]
	add	edx, ecx
	add	edx, DWORD PTR [edi+56]

; 964  :     if (len == 0) return 0;

	cmp	eax, ebx

; 1328 : 
; 1329 :         /* If there was no sliding:
; 1330 :          *    strstart <= WSIZE+MAX_DIST-1 && lookahead <= MIN_LOOKAHEAD - 1 &&
; 1331 :          *    more == window_size - lookahead - strstart
; 1332 :          * => more >= window_size - (MIN_LOOKAHEAD-1 + WSIZE + MAX_DIST-1)
; 1333 :          * => more >= window_size - 2*WSIZE + 2
; 1334 :          * In the BIG_MEM or MMAP case (not yet supported),
; 1335 :          *   window_size == input_size + MIN_LOOKAHEAD  &&
; 1336 :          *   strstart + s->lookahead <= input_size => more >= MIN_LOOKAHEAD.
; 1337 :          * Otherwise, window_size == 2*WSIZE so more >= 2.
; 1338 :          * If there was sliding, more >= WSIZE. So in all cases, more >= 2.
; 1339 :          */
; 1340 :         Assert(more >= 2, "more < 2");
; 1341 : 
; 1342 :         n = read_buf(s->strm, s->window + s->strstart + s->lookahead, more);

	mov	DWORD PTR _buf$1$[esp+12], edx

; 964  :     if (len == 0) return 0;

	cmovbe	ebx, eax
	test	ebx, ebx
	je	SHORT $LN24@fill_windo
$LN26@fill_windo:

; 965  : 
; 966  :     strm->avail_in  -= len;

	sub	eax, ebx
	mov	DWORD PTR [ebp+4], eax

; 967  : 
; 968  :     if (strm->state->wrap == 1) {

	mov	eax, DWORD PTR [ebp+28]
	mov	eax, DWORD PTR [eax+24]
	cmp	eax, 1
	jne	SHORT $LN27@fill_windo

; 969  :         strm->adler = adler32(strm->adler, strm->next_in, len);

	push	ebx
	push	DWORD PTR [ebp]
	push	DWORD PTR [ebp+48]
	call	_adler32

; 970  :     }

	jmp	SHORT $LN44@fill_windo
$LN27@fill_windo:

; 971  : #ifdef GZIP
; 972  :     else if (strm->state->wrap == 2) {

	cmp	eax, 2
	jne	SHORT $LN29@fill_windo

; 973  :         strm->adler = crc32(strm->adler, strm->next_in, len);

	push	ebx
	push	DWORD PTR [ebp]
	push	DWORD PTR [ebp+48]
	call	_crc32
$LN44@fill_windo:

; 974  :     }
; 975  : #endif
; 976  :     zmemcpy(buf, strm->next_in, len);

	mov	edx, DWORD PTR _buf$1$[esp+24]
	add	esp, 12					; 0000000cH
	mov	DWORD PTR [ebp+48], eax
$LN29@fill_windo:
	push	ebx
	push	DWORD PTR [ebp]
	push	edx
	call	_memcpy

; 977  :     strm->next_in  += len;

	add	DWORD PTR [ebp], ebx
	add	esp, 12					; 0000000cH

; 978  :     strm->total_in += len;

	add	DWORD PTR [ebp+8], ebx

; 979  : 
; 980  :     return (int)len;

	mov	ecx, DWORD PTR [edi+116]
$LN24@fill_windo:

; 1343 :         s->lookahead += n;

	lea	ebp, DWORD PTR [ecx+ebx]
	mov	DWORD PTR [edi+116], ebp

; 1344 : 
; 1345 :         /* Initialize the hash value now that we have some input: */
; 1346 :         if (s->lookahead >= MIN_MATCH) {

	cmp	ebp, 3
	jb	SHORT $LN2@fill_windo

; 1347 :             s->ins_h = s->window[s->strstart];

	mov	eax, DWORD PTR [edi+56]
	add	eax, DWORD PTR [edi+108]

; 1348 :             UPDATE_HASH(s, s->ins_h, s->window[s->strstart+1]);

	mov	ecx, DWORD PTR [edi+88]
	movzx	edx, BYTE PTR [eax]
	mov	DWORD PTR [edi+72], edx
	movzx	eax, BYTE PTR [eax+1]
	shl	edx, cl
	xor	edx, eax
	and	edx, DWORD PTR [edi+84]
	mov	DWORD PTR [edi+72], edx
$LN2@fill_windo:

; 1349 : #if MIN_MATCH != 3
; 1350 :             Call UPDATE_HASH() MIN_MATCH-3 more times
; 1351 : #endif
; 1352 :         }
; 1353 :         /* If the whole input has less than MIN_MATCH bytes, ins_h is garbage,
; 1354 :          * but this is not important since only literal bytes will be emitted.
; 1355 :          */
; 1356 : 
; 1357 :     } while (s->lookahead < MIN_LOOKAHEAD && s->strm->avail_in != 0);

	cmp	ebp, 262				; 00000106H
	jae	SHORT $LN3@fill_windo
	mov	eax, DWORD PTR [edi]
	cmp	DWORD PTR [eax+4], 0
	jne	$LL4@fill_windo
$LN3@fill_windo:
	pop	edi

; 1358 : }

	pop	esi
	pop	ebp
	pop	ebx
	ret	0
_fill_window ENDP
_TEXT	ENDS
; Function compile flags: /Ogtpy
; File d:\repo\msx-master\msx\bluemsx-dpi\src\unzip\deflate.c
;	COMDAT _deflateInit2_
_TEXT	SEGMENT
_strm$ = 8						; size = 4
_level$ = 12						; size = 4
_method$ = 16						; size = 4
_windowBits$ = 20					; size = 4
_memLevel$ = 24						; size = 4
_strategy$ = 28						; size = 4
tv763 = 32						; size = 4
_version$ = 32						; size = 4
_stream_size$ = 36					; size = 4
_deflateInit2_ PROC					; COMDAT

; 227  :     deflate_state *s;
; 228  :     int wrap = 1;
; 229  :     static const char my_version[] = ZLIB_VERSION;
; 230  : 
; 231  :     ushf *overlay;
; 232  :     /* We overlay pending_buf and d_buf+l_buf. This works since the average
; 233  :      * output size for (length,distance) codes is <= 24 bits.
; 234  :      */
; 235  : 
; 236  :     if (version == Z_NULL || version[0] != my_version[0] ||

	mov	eax, DWORD PTR _version$[esp-4]
	push	esi
	mov	esi, 1
	test	eax, eax
	je	$LN3@deflateIni
	cmp	BYTE PTR [eax], 49			; 00000031H
	jne	$LN3@deflateIni
	cmp	DWORD PTR _stream_size$[esp], 56	; 00000038H
	jne	$LN3@deflateIni

; 239  :     }
; 240  :     if (strm == Z_NULL) return Z_STREAM_ERROR;

	push	ebx
	push	edi
	mov	edi, DWORD PTR _strm$[esp+8]
	test	edi, edi
	je	$LN12@deflateIni

; 241  : 
; 242  :     strm->msg = Z_NULL;
; 243  :     if (strm->zalloc == (alloc_func)0) {

	cmp	DWORD PTR [edi+32], 0
	mov	DWORD PTR [edi+24], 0
	jne	SHORT $LN5@deflateIni

; 244  :         strm->zalloc = zcalloc;

	mov	DWORD PTR [edi+32], OFFSET _zcalloc

; 245  :         strm->opaque = (voidpf)0;

	mov	DWORD PTR [edi+40], 0
$LN5@deflateIni:

; 246  :     }
; 247  :     if (strm->zfree == (free_func)0) strm->zfree = zcfree;

	cmp	DWORD PTR [edi+36], 0
	jne	SHORT $LN6@deflateIni
	mov	DWORD PTR [edi+36], OFFSET _zcfree
$LN6@deflateIni:

; 248  : 
; 249  : #ifdef FASTEST
; 250  :     if (level != 0) level = 1;
; 251  : #else
; 252  :     if (level == Z_DEFAULT_COMPRESSION) level = 6;
; 253  : #endif
; 254  : 
; 255  :     if (windowBits < 0) { /* suppress zlib wrapper */

	cmp	DWORD PTR _level$[esp+8], -1
	mov	ecx, 6
	mov	ebx, DWORD PTR _windowBits$[esp+8]
	cmovne	ecx, DWORD PTR _level$[esp+8]
	mov	DWORD PTR tv763[esp+8], ecx
	test	ebx, ebx
	jns	SHORT $LN8@deflateIni

; 256  :         wrap = 0;

	xor	esi, esi

; 257  :         windowBits = -windowBits;

	neg	ebx

; 258  :     }

	jmp	SHORT $LN10@deflateIni
$LN8@deflateIni:

; 259  : #ifdef GZIP
; 260  :     else if (windowBits > 15) {

	cmp	ebx, 15					; 0000000fH
	jle	SHORT $LN10@deflateIni

; 261  :         wrap = 2;       /* write gzip wrapper instead */

	mov	esi, 2

; 262  :         windowBits -= 16;

	sub	ebx, 16					; 00000010H
$LN10@deflateIni:

; 263  :     }
; 264  : #endif
; 265  :     if (memLevel < 1 || memLevel > MAX_MEM_LEVEL || method != Z_DEFLATED ||
; 266  :         windowBits < 8 || windowBits > 15 || level < 0 || level > 9 ||
; 267  :         strategy < 0 || strategy > Z_FIXED) {

	mov	eax, DWORD PTR _memLevel$[esp+8]
	dec	eax
	cmp	eax, 8
	ja	$LN12@deflateIni
	cmp	DWORD PTR _method$[esp+8], 8
	jne	$LN12@deflateIni
	lea	eax, DWORD PTR [ebx-8]
	cmp	eax, 7
	ja	$LN12@deflateIni
	cmp	ecx, 9
	ja	$LN12@deflateIni
	cmp	DWORD PTR _strategy$[esp+8], 4
	ja	$LN12@deflateIni

; 269  :     }
; 270  :     if (windowBits == 8) windowBits = 9;  /* until 256-byte window bug fixed */

	push	ebp

; 271  :     s = (deflate_state *) ZALLOC(strm, 1, sizeof(deflate_state));

	push	5824					; 000016c0H
	mov	eax, 9
	cmp	ebx, 8
	push	1
	push	DWORD PTR [edi+40]
	cmove	ebx, eax
	mov	eax, DWORD PTR [edi+32]
	call	eax
	mov	ebp, eax
	add	esp, 12					; 0000000cH

; 272  :     if (s == Z_NULL) return Z_MEM_ERROR;

	test	ebp, ebp
	je	$LN31@deflateIni

; 273  :     strm->state = (struct internal_state FAR *)s;

	mov	DWORD PTR [edi+28], ebp

; 274  :     s->strm = strm;
; 275  : 
; 276  :     s->wrap = wrap;
; 277  :     s->gzhead = Z_NULL;
; 278  :     s->w_bits = windowBits;
; 279  :     s->w_size = 1 << s->w_bits;

	mov	ecx, ebx
	mov	DWORD PTR [ebp+24], esi
	mov	esi, 1
	shl	esi, cl
	mov	DWORD PTR [ebp+48], ebx

; 280  :     s->w_mask = s->w_size - 1;
; 281  : 
; 282  :     s->hash_bits = memLevel + 7;

	mov	ebx, DWORD PTR _memLevel$[esp+12]

; 283  :     s->hash_size = 1 << s->hash_bits;
; 284  :     s->hash_mask = s->hash_size - 1;
; 285  :     s->hash_shift =  ((s->hash_bits+MIN_MATCH-1)/MIN_MATCH);
; 286  : 
; 287  :     s->window = (Bytef *) ZALLOC(strm, s->w_size, 2*sizeof(Byte));

	push	2
	lea	eax, DWORD PTR [esi-1]
	mov	DWORD PTR [ebp], edi
	mov	DWORD PTR [ebp+52], eax
	mov	eax, 1
	lea	ecx, DWORD PTR [ebx+7]
	mov	DWORD PTR [ebp+28], 0
	shl	eax, cl
	mov	DWORD PTR [ebp+76], eax
	dec	eax
	mov	DWORD PTR [ebp+84], eax
	mov	eax, -1431655765			; aaaaaaabH
	mov	DWORD PTR [ebp+80], ecx
	add	ecx, 2
	mul	ecx
	mov	DWORD PTR [ebp+44], esi
	shr	edx, 1
	mov	DWORD PTR [ebp+88], edx
	mov	eax, DWORD PTR [edi+32]
	push	esi
	push	DWORD PTR [edi+40]
	call	eax

; 288  :     s->prev   = (Posf *)  ZALLOC(strm, s->w_size, sizeof(Pos));

	push	2
	push	DWORD PTR [ebp+44]
	mov	DWORD PTR [ebp+56], eax
	push	DWORD PTR [edi+40]
	mov	eax, DWORD PTR [edi+32]
	call	eax

; 289  :     s->head   = (Posf *)  ZALLOC(strm, s->hash_size, sizeof(Pos));

	push	2
	push	DWORD PTR [ebp+76]
	mov	DWORD PTR [ebp+64], eax
	push	DWORD PTR [edi+40]
	mov	eax, DWORD PTR [edi+32]
	call	eax
	mov	DWORD PTR [ebp+68], eax

; 290  : 
; 291  :     s->lit_bufsize = 1 << (memLevel + 6); /* 16K elements by default */

	lea	ecx, DWORD PTR [ebx+6]
	mov	eax, 1
	shl	eax, cl

; 292  : 
; 293  :     overlay = (ushf *) ZALLOC(strm, s->lit_bufsize, sizeof(ush)+2);

	push	4
	push	eax
	mov	DWORD PTR [ebp+5788], eax
	push	DWORD PTR [edi+40]
	mov	eax, DWORD PTR [edi+32]
	call	eax

; 294  :     s->pending_buf = (uchf *) overlay;
; 295  :     s->pending_buf_size = (ulg)s->lit_bufsize * (sizeof(ush)+2L);

	mov	esi, DWORD PTR [ebp+5788]
	add	esp, 48					; 00000030H

; 296  : 
; 297  :     if (s->window == Z_NULL || s->prev == Z_NULL || s->head == Z_NULL ||

	cmp	DWORD PTR [ebp+56], 0
	mov	edx, eax
	mov	DWORD PTR [ebp+8], edx
	lea	ecx, DWORD PTR [esi*4]
	mov	DWORD PTR [ebp+12], ecx
	je	$LN16@deflateIni
	cmp	DWORD PTR [ebp+64], 0
	je	$LN16@deflateIni
	cmp	DWORD PTR [ebp+68], 0
	je	$LN16@deflateIni
	test	edx, edx
	je	$LN16@deflateIni

; 303  :     }
; 304  :     s->d_buf = overlay + s->lit_bufsize/sizeof(ush);

	mov	eax, esi

; 305  :     s->l_buf = s->pending_buf + (1+sizeof(ush))*s->lit_bufsize;
; 306  : 
; 307  :     s->level = level;
; 308  :     s->strategy = strategy;
; 309  :     s->method = (Byte)method;

	mov	BYTE PTR [ebp+36], 8
	shr	eax, 1
	lea	eax, DWORD PTR [edx+eax*2]
	mov	DWORD PTR [ebp+5796], eax
	lea	eax, DWORD PTR [edx+esi*2]
	add	eax, esi
	mov	DWORD PTR [ebp+5784], eax
	mov	eax, DWORD PTR tv763[esp+12]
	mov	DWORD PTR [ebp+132], eax
	mov	eax, DWORD PTR _strategy$[esp+12]
	mov	DWORD PTR [ebp+136], eax

; 363  :         strm->zalloc == (alloc_func)0 || strm->zfree == (free_func)0) {

	mov	esi, DWORD PTR [edi+28]
	test	esi, esi
	je	$LN20@deflateIni
	cmp	DWORD PTR [edi+32], 0
	je	$LN20@deflateIni
	cmp	DWORD PTR [edi+36], 0
	je	$LN20@deflateIni

; 365  :     }
; 366  : 
; 367  :     strm->total_in = strm->total_out = 0;

	mov	DWORD PTR [edi+20], 0
	mov	DWORD PTR [edi+8], 0

; 368  :     strm->msg = Z_NULL; /* use zfree if we ever allocate msg dynamically */

	mov	DWORD PTR [edi+24], 0

; 369  :     strm->data_type = Z_UNKNOWN;

	mov	DWORD PTR [edi+44], 2

; 370  : 
; 371  :     s = (deflate_state *)strm->state;
; 372  :     s->pending = 0;
; 373  :     s->pending_out = s->pending_buf;
; 374  : 
; 375  :     if (s->wrap < 0) {

	mov	ecx, DWORD PTR [esi+24]
	mov	eax, DWORD PTR [esi+8]
	mov	DWORD PTR [esi+20], 0
	mov	DWORD PTR [esi+16], eax
	test	ecx, ecx
	jns	SHORT $LN21@deflateIni

; 376  :         s->wrap = -s->wrap; /* was made negative by deflate(..., Z_FINISH); */

	neg	ecx
	mov	DWORD PTR [esi+24], ecx
$LN21@deflateIni:

; 377  :     }
; 378  :     s->status = s->wrap ? INIT_STATE : BUSY_STATE;

	test	ecx, ecx
	mov	eax, 113				; 00000071H

; 379  :     strm->adler =

	push	0
	mov	edx, 42					; 0000002aH
	cmovne	eax, edx
	mov	DWORD PTR [esi+4], eax
	push	0
	push	0
	cmp	ecx, 2
	jne	SHORT $LN23@deflateIni
	call	_crc32
	jmp	SHORT $LN30@deflateIni
$LN23@deflateIni:
	call	_adler32
$LN30@deflateIni:
	add	esp, 12					; 0000000cH
	mov	DWORD PTR [edi+48], eax

; 380  : #ifdef GZIP
; 381  :         s->wrap == 2 ? crc32(0L, Z_NULL, 0) :
; 382  : #endif
; 383  :         adler32(0L, Z_NULL, 0);
; 384  :     s->last_flush = Z_NO_FLUSH;

	mov	DWORD PTR [esi+40], 0

; 385  : 
; 386  :     _tr_init(s);

	push	esi
	call	__tr_init

; 989  :     s->window_size = (ulg)2L*s->w_size;

	mov	eax, DWORD PTR [esi+44]

; 990  : 
; 991  :     CLEAR_HASH(s);

	xor	edx, edx
	mov	ecx, DWORD PTR [esi+76]
	add	eax, eax
	mov	DWORD PTR [esi+60], eax
	mov	eax, DWORD PTR [esi+68]
	mov	WORD PTR [eax+ecx*2-2], dx
	mov	eax, DWORD PTR [esi+76]
	lea	eax, DWORD PTR [eax*2-2]
	push	eax
	push	edx
	push	DWORD PTR [esi+68]
	call	_memset

; 992  : 
; 993  :     /* Set the default configuration parameters:
; 994  :      */
; 995  :     s->max_lazy_match   = configuration_table[s->level].max_lazy;

	mov	eax, DWORD PTR [esi+132]
	add	esp, 16					; 00000010H
	lea	ecx, DWORD PTR [eax+eax*2]
	movzx	eax, WORD PTR _configuration_table[ecx*4+2]
	mov	DWORD PTR [esi+128], eax

; 996  :     s->good_match       = configuration_table[s->level].good_length;

	movzx	eax, WORD PTR _configuration_table[ecx*4]
	mov	DWORD PTR [esi+140], eax

; 997  :     s->nice_match       = configuration_table[s->level].nice_length;

	movzx	eax, WORD PTR _configuration_table[ecx*4+4]
	pop	ebp
	mov	DWORD PTR [esi+144], eax

; 998  :     s->max_chain_length = configuration_table[s->level].max_chain;

	movzx	eax, WORD PTR _configuration_table[ecx*4+6]
	pop	edi
	mov	DWORD PTR [esi+124], eax

; 389  :     return Z_OK;

	xor	eax, eax
	pop	ebx

; 1000 :     s->strstart = 0;

	mov	DWORD PTR [esi+108], 0

; 1001 :     s->block_start = 0L;

	mov	DWORD PTR [esi+92], 0

; 1002 :     s->lookahead = 0;

	mov	DWORD PTR [esi+116], 0

; 1003 :     s->match_length = s->prev_length = MIN_MATCH-1;

	mov	DWORD PTR [esi+120], 2
	mov	DWORD PTR [esi+96], 2

; 1004 :     s->match_available = 0;

	mov	DWORD PTR [esi+104], 0

; 1005 :     s->ins_h = 0;

	mov	DWORD PTR [esi+72], 0
	pop	esi

; 310  : 
; 311  :     return deflateReset(strm);
; 312  : }

	ret	0
$LN20@deflateIni:
	pop	ebp
	pop	edi
	pop	ebx

; 364  :         return Z_STREAM_ERROR;

	mov	eax, -2					; fffffffeH
	pop	esi

; 310  : 
; 311  :     return deflateReset(strm);
; 312  : }

	ret	0
$LN16@deflateIni:

; 298  :         s->pending_buf == Z_NULL) {
; 299  :         s->status = FINISH_STATE;

	mov	DWORD PTR [ebp+4], 666			; 0000029aH

; 300  :         strm->msg = (char*)ERR_MSG(Z_MEM_ERROR);

	mov	eax, DWORD PTR _z_errmsg+24

; 301  :         deflateEnd (strm);

	push	edi
	mov	DWORD PTR [edi+24], eax
	call	_deflateEnd
	add	esp, 4
$LN31@deflateIni:

; 302  :         return Z_MEM_ERROR;

	pop	ebp
	pop	edi
	pop	ebx
	mov	eax, -4					; fffffffcH
	pop	esi

; 310  : 
; 311  :     return deflateReset(strm);
; 312  : }

	ret	0
$LN12@deflateIni:
	pop	edi
	pop	ebx

; 268  :         return Z_STREAM_ERROR;

	mov	eax, -2					; fffffffeH
	pop	esi

; 310  : 
; 311  :     return deflateReset(strm);
; 312  : }

	ret	0
$LN3@deflateIni:

; 237  :         stream_size != sizeof(z_stream)) {
; 238  :         return Z_VERSION_ERROR;

	mov	eax, -6					; fffffffaH
	pop	esi

; 310  : 
; 311  :     return deflateReset(strm);
; 312  : }

	ret	0
_deflateInit2_ ENDP
_TEXT	ENDS
; Function compile flags: /Ogtpy
; File d:\repo\msx-master\msx\bluemsx-dpi\src\unzip\deflate.c
;	COMDAT _deflateInit_
_TEXT	SEGMENT
_strm$ = 8						; size = 4
_level$ = 12						; size = 4
_version$ = 16						; size = 4
_stream_size$ = 20					; size = 4
_deflateInit_ PROC					; COMDAT

; 210  :     return deflateInit2_(strm, level, Z_DEFLATED, MAX_WBITS, DEF_MEM_LEVEL,

	push	DWORD PTR _stream_size$[esp-4]
	push	DWORD PTR _version$[esp]
	push	0
	push	8
	push	15					; 0000000fH
	push	8
	push	DWORD PTR _level$[esp+20]
	push	DWORD PTR _strm$[esp+24]
	call	_deflateInit2_
	add	esp, 32					; 00000020H

; 211  :                          Z_DEFAULT_STRATEGY, version, stream_size);
; 212  :     /* To do: ignore strm->next_in if we use it as window */
; 213  : }

	ret	0
_deflateInit_ ENDP
_TEXT	ENDS
; Function compile flags: /Ogtpy
; File d:\repo\msx-master\msx\bluemsx-dpi\src\unzip\deflate.c
;	COMDAT _deflateSetHeader
_TEXT	SEGMENT
_strm$ = 8						; size = 4
_head$ = 12						; size = 4
_deflateSetHeader PROC					; COMDAT

; 397  :     if (strm == Z_NULL || strm->state == Z_NULL) return Z_STREAM_ERROR;

	mov	ecx, DWORD PTR _strm$[esp-4]
	test	ecx, ecx
	je	SHORT $LN3@deflateSet
	mov	ecx, DWORD PTR [ecx+28]
	test	ecx, ecx
	je	SHORT $LN3@deflateSet

; 398  :     if (strm->state->wrap != 2) return Z_STREAM_ERROR;

	cmp	DWORD PTR [ecx+24], 2
	jne	SHORT $LN3@deflateSet

; 399  :     strm->state->gzhead = head;

	mov	eax, DWORD PTR _head$[esp-4]
	mov	DWORD PTR [ecx+28], eax

; 400  :     return Z_OK;

	xor	eax, eax

; 401  : }

	ret	0
$LN3@deflateSet:

; 397  :     if (strm == Z_NULL || strm->state == Z_NULL) return Z_STREAM_ERROR;

	mov	eax, -2					; fffffffeH

; 401  : }

	ret	0
_deflateSetHeader ENDP
_TEXT	ENDS
; Function compile flags: /Ogtpy
; File d:\repo\msx-master\msx\bluemsx-dpi\src\unzip\deflate.c
;	COMDAT _deflatePrime
_TEXT	SEGMENT
_strm$ = 8						; size = 4
_bits$ = 12						; size = 4
_value$ = 16						; size = 4
_deflatePrime PROC					; COMDAT

; 409  :     if (strm == Z_NULL || strm->state == Z_NULL) return Z_STREAM_ERROR;

	mov	eax, DWORD PTR _strm$[esp-4]
	test	eax, eax
	je	SHORT $LN3@deflatePri
	mov	edx, DWORD PTR [eax+28]
	test	edx, edx
	je	SHORT $LN3@deflatePri

; 410  :     strm->state->bi_valid = bits;

	mov	ecx, DWORD PTR _bits$[esp-4]
	mov	DWORD PTR [edx+5820], ecx

; 411  :     strm->state->bi_buf = (ush)(value & ((1 << bits) - 1));

	mov	edx, 1
	mov	eax, DWORD PTR [eax+28]
	shl	dx, cl
	dec	dx
	and	dx, WORD PTR _value$[esp-4]
	mov	WORD PTR [eax+5816], dx

; 412  :     return Z_OK;

	xor	eax, eax

; 413  : }

	ret	0
$LN3@deflatePri:

; 409  :     if (strm == Z_NULL || strm->state == Z_NULL) return Z_STREAM_ERROR;

	mov	eax, -2					; fffffffeH

; 413  : }

	ret	0
_deflatePrime ENDP
_TEXT	ENDS
; Function compile flags: /Ogtpy
; File d:\repo\msx-master\msx\bluemsx-dpi\src\unzip\deflate.c
;	COMDAT _deflateBound
_TEXT	SEGMENT
_strm$ = 8						; size = 4
_sourceLen$ = 12					; size = 4
_deflateBound PROC					; COMDAT

; 493  :     deflate_state *s;
; 494  :     uLong destLen;
; 495  : 
; 496  :     /* conservative upper bound */
; 497  :     destLen = sourceLen +

	mov	edx, DWORD PTR _sourceLen$[esp-4]
	lea	ecx, DWORD PTR [edx+63]
	shr	ecx, 6
	lea	eax, DWORD PTR [edx+7]
	shr	eax, 3
	add	ecx, edx
	add	ecx, eax

; 498  :               ((sourceLen + 7) >> 3) + ((sourceLen + 63) >> 6) + 11;
; 499  : 
; 500  :     /* if can't get parameters, return conservative bound */
; 501  :     if (strm == Z_NULL || strm->state == Z_NULL)

	mov	eax, DWORD PTR _strm$[esp-4]
	test	eax, eax
	je	SHORT $LN3@deflateBou
	mov	eax, DWORD PTR [eax+28]
	test	eax, eax
	je	SHORT $LN3@deflateBou

; 503  : 
; 504  :     /* if not default parameters, return conservative bound */
; 505  :     s = strm->state;
; 506  :     if (s->w_bits != 15 || s->hash_bits != 8 + 7)

	cmp	DWORD PTR [eax+48], 15			; 0000000fH
	jne	SHORT $LN3@deflateBou
	cmp	DWORD PTR [eax+80], 15			; 0000000fH
	jne	SHORT $LN3@deflateBou

; 507  :         return destLen;
; 508  : 
; 509  :     /* default settings: return tight bound for that case */
; 510  :     return compressBound(sourceLen);

	push	edx
	call	_compressBound
	add	esp, 4

; 511  : }

	ret	0
$LN3@deflateBou:

; 502  :         return destLen;

	lea	eax, DWORD PTR [ecx+11]

; 511  : }

	ret	0
_deflateBound ENDP
_TEXT	ENDS
; Function compile flags: /Ogtpy
; File d:\repo\msx-master\msx\bluemsx-dpi\src\unzip\deflate.c
;	COMDAT _deflateTune
_TEXT	SEGMENT
_strm$ = 8						; size = 4
_good_length$ = 12					; size = 4
_max_lazy$ = 16						; size = 4
_nice_length$ = 20					; size = 4
_max_chain$ = 24					; size = 4
_deflateTune PROC					; COMDAT

; 461  :     deflate_state *s;
; 462  : 
; 463  :     if (strm == Z_NULL || strm->state == Z_NULL) return Z_STREAM_ERROR;

	mov	ecx, DWORD PTR _strm$[esp-4]
	test	ecx, ecx
	je	SHORT $LN3@deflateTun
	mov	ecx, DWORD PTR [ecx+28]
	test	ecx, ecx
	je	SHORT $LN3@deflateTun

; 464  :     s = strm->state;
; 465  :     s->good_match = good_length;

	mov	eax, DWORD PTR _good_length$[esp-4]
	mov	DWORD PTR [ecx+140], eax

; 466  :     s->max_lazy_match = max_lazy;

	mov	eax, DWORD PTR _max_lazy$[esp-4]
	mov	DWORD PTR [ecx+128], eax

; 467  :     s->nice_match = nice_length;

	mov	eax, DWORD PTR _nice_length$[esp-4]
	mov	DWORD PTR [ecx+144], eax

; 468  :     s->max_chain_length = max_chain;

	mov	eax, DWORD PTR _max_chain$[esp-4]
	mov	DWORD PTR [ecx+124], eax

; 469  :     return Z_OK;

	xor	eax, eax

; 470  : }

	ret	0
$LN3@deflateTun:

; 461  :     deflate_state *s;
; 462  : 
; 463  :     if (strm == Z_NULL || strm->state == Z_NULL) return Z_STREAM_ERROR;

	mov	eax, -2					; fffffffeH

; 470  : }

	ret	0
_deflateTune ENDP
_TEXT	ENDS
; Function compile flags: /Ogtpy
; File d:\repo\msx-master\msx\bluemsx-dpi\src\unzip\deflate.c
;	COMDAT _deflateParams
_TEXT	SEGMENT
_err$1$ = -4						; size = 4
_strm$ = 8						; size = 4
_level$ = 12						; size = 4
_strategy$ = 16						; size = 4
_deflateParams PROC					; COMDAT

; 420  : {

	push	ecx

; 421  :     deflate_state *s;
; 422  :     compress_func func;
; 423  :     int err = Z_OK;
; 424  : 
; 425  :     if (strm == Z_NULL || strm->state == Z_NULL) return Z_STREAM_ERROR;

	mov	ecx, DWORD PTR _strm$[esp]
	mov	DWORD PTR _err$1$[esp+4], 0
	push	ebp
	mov	ebp, DWORD PTR _strategy$[esp+4]
	push	esi
	push	edi
	test	ecx, ecx
	je	$LN3@deflatePar
	mov	esi, DWORD PTR [ecx+28]
	test	esi, esi
	je	$LN3@deflatePar

; 426  :     s = strm->state;
; 427  : 
; 428  : #ifdef FASTEST
; 429  :     if (level != 0) level = 1;
; 430  : #else
; 431  :     if (level == Z_DEFAULT_COMPRESSION) level = 6;
; 432  : #endif
; 433  :     if (level < 0 || level > 9 || strategy < 0 || strategy > Z_FIXED) {

	cmp	DWORD PTR _level$[esp+12], -1
	mov	edi, 6
	cmovne	edi, DWORD PTR _level$[esp+12]
	cmp	edi, 9
	ja	$LN3@deflatePar
	cmp	ebp, 4
	ja	$LN3@deflatePar

; 434  :         return Z_STREAM_ERROR;
; 435  :     }
; 436  :     func = configuration_table[s->level].func;

	mov	edx, DWORD PTR [esi+132]
	push	ebx

; 437  : 
; 438  :     if (func != configuration_table[level].func && strm->total_in != 0) {

	lea	ebx, DWORD PTR [edi+edi*2]
	lea	eax, DWORD PTR [edx+edx*2]
	mov	eax, DWORD PTR _configuration_table[eax*4+8]
	cmp	eax, DWORD PTR _configuration_table[ebx*4+8]
	je	SHORT $LN7@deflatePar
	cmp	DWORD PTR [ecx+8], 0
	je	SHORT $LN7@deflatePar

; 439  :         /* Flush the last buffer: */
; 440  :         err = deflate(strm, Z_PARTIAL_FLUSH);

	push	1
	push	ecx
	call	_deflate
	mov	edx, DWORD PTR [esi+132]
	add	esp, 8
	mov	DWORD PTR _err$1$[esp+20], eax
$LN7@deflatePar:

; 441  :     }
; 442  :     if (s->level != level) {

	cmp	edx, edi
	je	SHORT $LN8@deflatePar

; 443  :         s->level = level;

	mov	DWORD PTR [esi+132], edi

; 444  :         s->max_lazy_match   = configuration_table[level].max_lazy;

	movzx	ecx, WORD PTR _configuration_table[ebx*4+2]
	mov	DWORD PTR [esi+128], ecx

; 445  :         s->good_match       = configuration_table[level].good_length;

	movzx	eax, WORD PTR _configuration_table[ebx*4]
	mov	DWORD PTR [esi+140], eax

; 446  :         s->nice_match       = configuration_table[level].nice_length;

	movzx	eax, WORD PTR _configuration_table[ebx*4+4]
	mov	DWORD PTR [esi+144], eax

; 447  :         s->max_chain_length = configuration_table[level].max_chain;

	movzx	eax, WORD PTR _configuration_table[ebx*4+6]
	mov	DWORD PTR [esi+124], eax
$LN8@deflatePar:

; 448  :     }
; 449  :     s->strategy = strategy;
; 450  :     return err;

	mov	eax, DWORD PTR _err$1$[esp+20]
	pop	ebx
	pop	edi
	mov	DWORD PTR [esi+136], ebp

; 451  : }

	pop	esi
	pop	ebp
	pop	ecx
	ret	0
$LN3@deflatePar:
	pop	edi
	pop	esi
	mov	eax, -2					; fffffffeH
	pop	ebp
	pop	ecx
	ret	0
_deflateParams ENDP
_TEXT	ENDS
; Function compile flags: /Ogtpy
; File d:\repo\msx-master\msx\bluemsx-dpi\src\unzip\deflate.c
;	COMDAT _deflateReset
_TEXT	SEGMENT
_strm$ = 8						; size = 4
_deflateReset PROC					; COMDAT

; 359  : {

	push	esi
	push	edi

; 360  :     deflate_state *s;
; 361  : 
; 362  :     if (strm == Z_NULL || strm->state == Z_NULL ||
; 363  :         strm->zalloc == (alloc_func)0 || strm->zfree == (free_func)0) {

	mov	edi, DWORD PTR _strm$[esp+4]
	test	edi, edi
	je	$LN3@deflateRes
	mov	esi, DWORD PTR [edi+28]
	test	esi, esi
	je	$LN3@deflateRes
	cmp	DWORD PTR [edi+32], 0
	je	$LN3@deflateRes
	cmp	DWORD PTR [edi+36], 0
	je	$LN3@deflateRes

; 365  :     }
; 366  : 
; 367  :     strm->total_in = strm->total_out = 0;

	mov	DWORD PTR [edi+20], 0
	mov	DWORD PTR [edi+8], 0

; 368  :     strm->msg = Z_NULL; /* use zfree if we ever allocate msg dynamically */

	mov	DWORD PTR [edi+24], 0

; 369  :     strm->data_type = Z_UNKNOWN;

	mov	DWORD PTR [edi+44], 2

; 370  : 
; 371  :     s = (deflate_state *)strm->state;
; 372  :     s->pending = 0;
; 373  :     s->pending_out = s->pending_buf;
; 374  : 
; 375  :     if (s->wrap < 0) {

	mov	ecx, DWORD PTR [esi+24]
	mov	eax, DWORD PTR [esi+8]
	mov	DWORD PTR [esi+20], 0
	mov	DWORD PTR [esi+16], eax
	test	ecx, ecx
	jns	SHORT $LN4@deflateRes

; 376  :         s->wrap = -s->wrap; /* was made negative by deflate(..., Z_FINISH); */

	neg	ecx
	mov	DWORD PTR [esi+24], ecx
$LN4@deflateRes:

; 377  :     }
; 378  :     s->status = s->wrap ? INIT_STATE : BUSY_STATE;

	test	ecx, ecx
	mov	eax, 113				; 00000071H

; 379  :     strm->adler =

	push	0
	mov	edx, 42					; 0000002aH
	cmovne	eax, edx
	mov	DWORD PTR [esi+4], eax
	push	0
	push	0
	cmp	ecx, 2
	jne	SHORT $LN6@deflateRes
	call	_crc32
	jmp	SHORT $LN10@deflateRes
$LN6@deflateRes:
	call	_adler32
$LN10@deflateRes:
	add	esp, 12					; 0000000cH
	mov	DWORD PTR [edi+48], eax

; 380  : #ifdef GZIP
; 381  :         s->wrap == 2 ? crc32(0L, Z_NULL, 0) :
; 382  : #endif
; 383  :         adler32(0L, Z_NULL, 0);
; 384  :     s->last_flush = Z_NO_FLUSH;

	mov	DWORD PTR [esi+40], 0

; 385  : 
; 386  :     _tr_init(s);

	push	esi
	call	__tr_init

; 989  :     s->window_size = (ulg)2L*s->w_size;

	mov	eax, DWORD PTR [esi+44]

; 990  : 
; 991  :     CLEAR_HASH(s);

	xor	edx, edx
	mov	ecx, DWORD PTR [esi+76]
	add	eax, eax
	mov	DWORD PTR [esi+60], eax
	mov	eax, DWORD PTR [esi+68]
	mov	WORD PTR [eax+ecx*2-2], dx
	mov	eax, DWORD PTR [esi+76]
	lea	eax, DWORD PTR [eax*2-2]
	push	eax
	push	edx
	push	DWORD PTR [esi+68]
	call	_memset

; 992  : 
; 993  :     /* Set the default configuration parameters:
; 994  :      */
; 995  :     s->max_lazy_match   = configuration_table[s->level].max_lazy;

	mov	eax, DWORD PTR [esi+132]
	add	esp, 16					; 00000010H
	lea	ecx, DWORD PTR [eax+eax*2]
	movzx	eax, WORD PTR _configuration_table[ecx*4+2]
	mov	DWORD PTR [esi+128], eax

; 996  :     s->good_match       = configuration_table[s->level].good_length;

	movzx	eax, WORD PTR _configuration_table[ecx*4]
	mov	DWORD PTR [esi+140], eax

; 997  :     s->nice_match       = configuration_table[s->level].nice_length;

	movzx	eax, WORD PTR _configuration_table[ecx*4+4]
	mov	DWORD PTR [esi+144], eax

; 998  :     s->max_chain_length = configuration_table[s->level].max_chain;

	movzx	eax, WORD PTR _configuration_table[ecx*4+6]
	mov	DWORD PTR [esi+124], eax

; 387  :     lm_init(s);
; 388  : 
; 389  :     return Z_OK;

	xor	eax, eax
	pop	edi

; 1000 :     s->strstart = 0;

	mov	DWORD PTR [esi+108], 0

; 1001 :     s->block_start = 0L;

	mov	DWORD PTR [esi+92], 0

; 1002 :     s->lookahead = 0;

	mov	DWORD PTR [esi+116], 0

; 1003 :     s->match_length = s->prev_length = MIN_MATCH-1;

	mov	DWORD PTR [esi+120], 2
	mov	DWORD PTR [esi+96], 2

; 1004 :     s->match_available = 0;

	mov	DWORD PTR [esi+104], 0

; 1005 :     s->ins_h = 0;

	mov	DWORD PTR [esi+72], 0

; 390  : }

	pop	esi
	ret	0
$LN3@deflateRes:
	pop	edi

; 364  :         return Z_STREAM_ERROR;

	mov	eax, -2					; fffffffeH

; 390  : }

	pop	esi
	ret	0
_deflateReset ENDP
_TEXT	ENDS
; Function compile flags: /Ogtpy
; File d:\repo\msx-master\msx\bluemsx-dpi\src\unzip\deflate.c
;	COMDAT _deflateCopy
_TEXT	SEGMENT
_dest$ = 8						; size = 4
_source$ = 12						; size = 4
_deflateCopy PROC					; COMDAT

; 898  : #ifdef MAXSEG_64K
; 899  :     return Z_STREAM_ERROR;
; 900  : #else
; 901  :     deflate_state *ds;
; 902  :     deflate_state *ss;
; 903  :     ushf *overlay;
; 904  : 
; 905  : 
; 906  :     if (source == Z_NULL || dest == Z_NULL || source->state == Z_NULL) {

	mov	eax, DWORD PTR _source$[esp-4]
	push	ebx
	push	esi
	test	eax, eax
	je	$LN3@deflateCop
	mov	esi, DWORD PTR _dest$[esp+4]
	test	esi, esi
	je	$LN3@deflateCop
	mov	ebx, DWORD PTR [eax+28]
	test	ebx, ebx
	je	$LN3@deflateCop

; 908  :     }
; 909  : 
; 910  :     ss = source->state;
; 911  : 
; 912  :     zmemcpy(dest, source, sizeof(z_stream));

	movups	xmm0, XMMWORD PTR [eax]
	push	edi

; 913  : 
; 914  :     ds = (deflate_state *) ZALLOC(dest, 1, sizeof(deflate_state));

	push	5824					; 000016c0H
	movups	XMMWORD PTR [esi], xmm0
	push	1
	movups	xmm0, XMMWORD PTR [eax+16]
	movups	XMMWORD PTR [esi+16], xmm0
	movups	xmm0, XMMWORD PTR [eax+32]
	movups	XMMWORD PTR [esi+32], xmm0
	movq	xmm0, QWORD PTR [eax+48]
	movq	QWORD PTR [esi+48], xmm0
	push	DWORD PTR [esi+40]
	mov	eax, DWORD PTR [esi+32]
	call	eax
	mov	edi, eax
	add	esp, 12					; 0000000cH

; 915  :     if (ds == Z_NULL) return Z_MEM_ERROR;

	test	edi, edi
	jne	SHORT $LN4@deflateCop
	lea	eax, DWORD PTR [edi-4]
	pop	edi
	pop	esi

; 946  : #endif /* MAXSEG_64K */
; 947  : }

	pop	ebx
	ret	0
$LN4@deflateCop:
	push	ebp

; 916  :     dest->state = (struct internal_state FAR *) ds;
; 917  :     zmemcpy(ds, ss, sizeof(deflate_state));

	push	5824					; 000016c0H
	push	ebx
	push	edi
	mov	DWORD PTR [esi+28], edi
	call	_memcpy

; 918  :     ds->strm = dest;
; 919  : 
; 920  :     ds->window = (Bytef *) ZALLOC(dest, ds->w_size, 2*sizeof(Byte));

	push	2
	push	DWORD PTR [edi+44]
	mov	DWORD PTR [edi], esi
	push	DWORD PTR [esi+40]
	mov	eax, DWORD PTR [esi+32]
	call	eax

; 921  :     ds->prev   = (Posf *)  ZALLOC(dest, ds->w_size, sizeof(Pos));

	push	2
	push	DWORD PTR [edi+44]
	mov	DWORD PTR [edi+56], eax
	push	DWORD PTR [esi+40]
	mov	eax, DWORD PTR [esi+32]
	call	eax

; 922  :     ds->head   = (Posf *)  ZALLOC(dest, ds->hash_size, sizeof(Pos));

	push	2
	push	DWORD PTR [edi+76]
	mov	DWORD PTR [edi+64], eax
	push	DWORD PTR [esi+40]
	mov	eax, DWORD PTR [esi+32]
	call	eax

; 923  :     overlay = (ushf *) ZALLOC(dest, ds->lit_bufsize, sizeof(ush)+2);

	push	4
	push	DWORD PTR [edi+5788]
	mov	DWORD PTR [edi+68], eax
	push	DWORD PTR [esi+40]
	mov	eax, DWORD PTR [esi+32]
	call	eax

; 924  :     ds->pending_buf = (uchf *) overlay;
; 925  : 
; 926  :     if (ds->window == Z_NULL || ds->prev == Z_NULL || ds->head == Z_NULL ||

	mov	ecx, DWORD PTR [edi+56]
	add	esp, 60					; 0000003cH
	mov	ebp, eax
	mov	DWORD PTR [edi+8], ebp
	test	ecx, ecx
	je	$LN6@deflateCop
	cmp	DWORD PTR [edi+64], 0
	je	$LN6@deflateCop
	cmp	DWORD PTR [edi+68], 0
	je	$LN6@deflateCop
	test	ebp, ebp
	je	$LN6@deflateCop

; 930  :     }
; 931  :     /* following zmemcpy do not work for 16-bit MSDOS */
; 932  :     zmemcpy(ds->window, ss->window, ds->w_size * 2 * sizeof(Byte));

	mov	eax, DWORD PTR [edi+44]
	add	eax, eax
	push	eax
	push	DWORD PTR [ebx+56]
	push	ecx
	call	_memcpy

; 933  :     zmemcpy(ds->prev, ss->prev, ds->w_size * sizeof(Pos));

	mov	eax, DWORD PTR [edi+44]
	add	eax, eax
	push	eax
	push	DWORD PTR [ebx+64]
	push	DWORD PTR [edi+64]
	call	_memcpy

; 934  :     zmemcpy(ds->head, ss->head, ds->hash_size * sizeof(Pos));

	mov	eax, DWORD PTR [edi+76]
	add	eax, eax
	push	eax
	push	DWORD PTR [ebx+68]
	push	DWORD PTR [edi+68]
	call	_memcpy

; 935  :     zmemcpy(ds->pending_buf, ss->pending_buf, (uInt)ds->pending_buf_size);

	push	DWORD PTR [edi+12]
	push	DWORD PTR [ebx+8]
	push	DWORD PTR [edi+8]
	call	_memcpy

; 936  : 
; 937  :     ds->pending_out = ds->pending_buf + (ss->pending_out - ss->pending_buf);

	mov	eax, DWORD PTR [ebx+16]
	add	esp, 48					; 00000030H
	sub	eax, DWORD PTR [ebx+8]
	mov	edx, DWORD PTR [edi+8]
	add	eax, edx

; 938  :     ds->d_buf = overlay + ds->lit_bufsize/sizeof(ush);

	mov	ecx, DWORD PTR [edi+5788]
	mov	DWORD PTR [edi+16], eax
	mov	eax, ecx
	shr	eax, 1
	lea	eax, DWORD PTR [eax*2]
	add	eax, ebp
	mov	DWORD PTR [edi+5796], eax

; 939  :     ds->l_buf = ds->pending_buf + (1+sizeof(ush))*ds->lit_bufsize;

	lea	eax, DWORD PTR [edx+ecx*2]
	add	eax, ecx
	mov	DWORD PTR [edi+5784], eax

; 940  : 
; 941  :     ds->l_desc.dyn_tree = ds->dyn_ltree;

	lea	eax, DWORD PTR [edi+148]
	mov	DWORD PTR [edi+2840], eax

; 942  :     ds->d_desc.dyn_tree = ds->dyn_dtree;

	lea	eax, DWORD PTR [edi+2440]
	mov	DWORD PTR [edi+2852], eax

; 943  :     ds->bl_desc.dyn_tree = ds->bl_tree;

	lea	eax, DWORD PTR [edi+2684]
	pop	ebp
	mov	DWORD PTR [edi+2864], eax

; 944  : 
; 945  :     return Z_OK;

	xor	eax, eax
	pop	edi
	pop	esi

; 946  : #endif /* MAXSEG_64K */
; 947  : }

	pop	ebx
	ret	0
$LN6@deflateCop:

; 927  :         ds->pending_buf == Z_NULL) {
; 928  :         deflateEnd (dest);

	push	esi
	call	_deflateEnd
	add	esp, 4

; 929  :         return Z_MEM_ERROR;

	mov	eax, -4					; fffffffcH
	pop	ebp
	pop	edi
	pop	esi

; 946  : #endif /* MAXSEG_64K */
; 947  : }

	pop	ebx
	ret	0
$LN3@deflateCop:
	pop	esi

; 907  :         return Z_STREAM_ERROR;

	mov	eax, -2					; fffffffeH

; 946  : #endif /* MAXSEG_64K */
; 947  : }

	pop	ebx
	ret	0
_deflateCopy ENDP
_TEXT	ENDS
; Function compile flags: /Ogtpy
; File d:\repo\msx-master\msx\bluemsx-dpi\src\unzip\deflate.c
;	COMDAT _deflateSetDictionary
_TEXT	SEGMENT
_strm$ = 8						; size = 4
_dictionary$ = 12					; size = 4
_dictLength$ = 16					; size = 4
_deflateSetDictionary PROC				; COMDAT

; 319  : {

	push	ebx
	push	ebp

; 320  :     deflate_state *s;
; 321  :     uInt length = dictLength;
; 322  :     uInt n;
; 323  :     IPos hash_head = 0;
; 324  : 
; 325  :     if (strm == Z_NULL || strm->state == Z_NULL || dictionary == Z_NULL ||
; 326  :         strm->state->wrap == 2 ||

	mov	ebp, DWORD PTR _strm$[esp+4]
	push	esi
	push	edi
	mov	edi, DWORD PTR _dictLength$[esp+12]
	test	ebp, ebp
	je	$LN6@deflateSet
	mov	esi, DWORD PTR [ebp+28]
	test	esi, esi
	je	$LN6@deflateSet
	mov	ebx, DWORD PTR _dictionary$[esp+12]
	test	ebx, ebx
	je	$LN6@deflateSet
	mov	eax, DWORD PTR [esi+24]
	cmp	eax, 2
	je	$LN6@deflateSet
	cmp	eax, 1
	jne	SHORT $LN5@deflateSet
	cmp	DWORD PTR [esi+4], 42			; 0000002aH
	jne	$LN6@deflateSet
$LN5@deflateSet:

; 327  :         (strm->state->wrap == 1 && strm->state->status != INIT_STATE))
; 328  :         return Z_STREAM_ERROR;
; 329  : 
; 330  :     s = strm->state;
; 331  :     if (s->wrap)

	test	eax, eax
	je	SHORT $LN7@deflateSet

; 332  :         strm->adler = adler32(strm->adler, dictionary, dictLength);

	push	edi
	push	ebx
	push	DWORD PTR [ebp+48]
	call	_adler32
	add	esp, 12					; 0000000cH
	mov	DWORD PTR [ebp+48], eax
$LN7@deflateSet:

; 333  : 
; 334  :     if (length < MIN_MATCH) return Z_OK;

	cmp	edi, 3
	jb	$LN17@deflateSet

; 335  :     if (length > MAX_DIST(s)) {

	mov	ebp, DWORD PTR [esi+44]
	add	ebp, -262				; fffffefaH
	cmp	edi, ebp
	jbe	SHORT $LN16@deflateSet

; 336  :         length = MAX_DIST(s);
; 337  :         dictionary += dictLength - length; /* use the tail of the dictionary */

	sub	edi, ebp
	add	ebx, edi
	jmp	SHORT $LN9@deflateSet
$LN16@deflateSet:

; 335  :     if (length > MAX_DIST(s)) {

	mov	ebp, edi
$LN9@deflateSet:

; 338  :     }
; 339  :     zmemcpy(s->window, dictionary, length);

	push	ebp
	push	ebx
	push	DWORD PTR [esi+56]
	call	_memcpy

; 340  :     s->strstart = length;
; 341  :     s->block_start = (long)length;
; 342  : 
; 343  :     /* Insert all strings in the hash table (except for the last two bytes).
; 344  :      * s->lookahead stays null, so s->ins_h will be recomputed at the next
; 345  :      * call of fill_window.
; 346  :      */
; 347  :     s->ins_h = s->window[0];

	mov	eax, DWORD PTR [esi+56]
	add	esp, 12					; 0000000cH

; 348  :     UPDATE_HASH(s, s->ins_h, s->window[1]);

	mov	ecx, DWORD PTR [esi+88]

; 349  :     for (n = 0; n <= length - MIN_MATCH; n++) {

	xor	ebx, ebx
	mov	DWORD PTR [esi+108], ebp
	mov	DWORD PTR [esi+92], ebp
	movzx	edx, BYTE PTR [eax]
	mov	DWORD PTR [esi+72], edx
	movzx	eax, BYTE PTR [eax+1]
	shl	edx, cl
	xor	edx, eax
	and	edx, DWORD PTR [esi+84]
	add	ebp, -3					; fffffffdH
	mov	DWORD PTR [esi+72], edx
	npad	6
$LL4@deflateSet:

; 350  :         INSERT_STRING(s, n, hash_head);

	mov	eax, DWORD PTR [esi+56]
	mov	ecx, DWORD PTR [esi+88]
	mov	edx, DWORD PTR [esi+52]
	and	edx, ebx
	movzx	edi, BYTE PTR [ebx+eax+2]
	mov	eax, DWORD PTR [esi+72]
	shl	eax, cl
	mov	ecx, DWORD PTR [esi+64]
	xor	edi, eax
	and	edi, DWORD PTR [esi+84]
	mov	eax, DWORD PTR [esi+68]
	mov	DWORD PTR [esi+72], edi
	mov	ax, WORD PTR [eax+edi*2]
	mov	WORD PTR [ecx+edx*2], ax
	mov	ecx, DWORD PTR [esi+72]
	mov	eax, DWORD PTR [esi+68]
	mov	WORD PTR [eax+ecx*2], bx
	inc	ebx
	cmp	ebx, ebp
	jbe	SHORT $LL4@deflateSet
$LN17@deflateSet:
	pop	edi

; 351  :     }
; 352  :     if (hash_head) hash_head = 0;  /* to make compiler happy */
; 353  :     return Z_OK;
; 354  : }

	pop	esi
	pop	ebp
	xor	eax, eax
	pop	ebx
	ret	0
$LN6@deflateSet:
	pop	edi
	pop	esi
	pop	ebp
	mov	eax, -2					; fffffffeH
	pop	ebx
	ret	0
_deflateSetDictionary ENDP
_TEXT	ENDS
; Function compile flags: /Ogtpy
; File d:\repo\msx-master\msx\bluemsx-dpi\src\unzip\deflate.c
;	COMDAT _deflateEnd
_TEXT	SEGMENT
_strm$ = 8						; size = 4
_deflateEnd PROC					; COMDAT

; 861  : {

	push	esi

; 862  :     int status;
; 863  : 
; 864  :     if (strm == Z_NULL || strm->state == Z_NULL) return Z_STREAM_ERROR;

	mov	esi, DWORD PTR _strm$[esp]
	push	edi
	test	esi, esi
	je	$LN3@deflateEnd
	mov	eax, DWORD PTR [esi+28]
	test	eax, eax
	je	$LN3@deflateEnd

; 865  : 
; 866  :     status = strm->state->status;

	mov	edi, DWORD PTR [eax+4]

; 867  :     if (status != INIT_STATE &&
; 868  :         status != EXTRA_STATE &&
; 869  :         status != NAME_STATE &&
; 870  :         status != COMMENT_STATE &&
; 871  :         status != HCRC_STATE &&
; 872  :         status != BUSY_STATE &&

	cmp	edi, 42					; 0000002aH
	je	SHORT $LN4@deflateEnd
	cmp	edi, 69					; 00000045H
	je	SHORT $LN4@deflateEnd
	cmp	edi, 73					; 00000049H
	je	SHORT $LN4@deflateEnd
	cmp	edi, 91					; 0000005bH
	je	SHORT $LN4@deflateEnd
	cmp	edi, 103				; 00000067H
	je	SHORT $LN4@deflateEnd
	cmp	edi, 113				; 00000071H
	je	SHORT $LN4@deflateEnd
	cmp	edi, 666				; 0000029aH
	jne	SHORT $LN3@deflateEnd
$LN4@deflateEnd:

; 873  :         status != FINISH_STATE) {
; 874  :       return Z_STREAM_ERROR;
; 875  :     }
; 876  : 
; 877  :     /* Deallocate in reverse order of allocations: */
; 878  :     TRY_FREE(strm, strm->state->pending_buf);

	mov	ecx, DWORD PTR [eax+8]
	test	ecx, ecx
	je	SHORT $LN5@deflateEnd
	mov	eax, DWORD PTR [esi+36]
	push	ecx
	push	DWORD PTR [esi+40]
	call	eax
	mov	eax, DWORD PTR [esi+28]
	add	esp, 8
$LN5@deflateEnd:

; 879  :     TRY_FREE(strm, strm->state->head);

	mov	ecx, DWORD PTR [eax+68]
	test	ecx, ecx
	je	SHORT $LN6@deflateEnd
	mov	eax, DWORD PTR [esi+36]
	push	ecx
	push	DWORD PTR [esi+40]
	call	eax
	mov	eax, DWORD PTR [esi+28]
	add	esp, 8
$LN6@deflateEnd:

; 880  :     TRY_FREE(strm, strm->state->prev);

	mov	ecx, DWORD PTR [eax+64]
	test	ecx, ecx
	je	SHORT $LN7@deflateEnd
	mov	eax, DWORD PTR [esi+36]
	push	ecx
	push	DWORD PTR [esi+40]
	call	eax
	mov	eax, DWORD PTR [esi+28]
	add	esp, 8
$LN7@deflateEnd:

; 881  :     TRY_FREE(strm, strm->state->window);

	mov	ecx, DWORD PTR [eax+56]
	test	ecx, ecx
	je	SHORT $LN8@deflateEnd
	mov	eax, DWORD PTR [esi+36]
	push	ecx
	push	DWORD PTR [esi+40]
	call	eax
	mov	eax, DWORD PTR [esi+28]
	add	esp, 8
$LN8@deflateEnd:

; 882  : 
; 883  :     ZFREE(strm, strm->state);

	push	eax
	push	DWORD PTR [esi+40]
	mov	eax, DWORD PTR [esi+36]
	call	eax
	add	esp, 8

; 884  :     strm->state = Z_NULL;

	mov	DWORD PTR [esi+28], 0

; 885  : 
; 886  :     return status == BUSY_STATE ? Z_DATA_ERROR : Z_OK;

	xor	eax, eax
	mov	ecx, -3					; fffffffdH
	cmp	edi, 113				; 00000071H
	pop	edi
	cmove	eax, ecx

; 887  : }

	pop	esi
	ret	0
$LN3@deflateEnd:
	pop	edi

; 862  :     int status;
; 863  : 
; 864  :     if (strm == Z_NULL || strm->state == Z_NULL) return Z_STREAM_ERROR;

	mov	eax, -2					; fffffffeH

; 887  : }

	pop	esi
	ret	0
_deflateEnd ENDP
_TEXT	ENDS
; Function compile flags: /Ogtpy
; File d:\repo\msx-master\msx\bluemsx-dpi\src\unzip\deflate.c
;	COMDAT _deflate
_TEXT	SEGMENT
_old_flush$1$ = -4					; size = 4
_val$1$ = 8						; size = 4
_val$1$ = 8						; size = 4
_strm$ = 8						; size = 4
_flush$ = 12						; size = 4
_deflate PROC						; COMDAT

; 555  : {

	push	ecx
	mov	ecx, DWORD PTR _flush$[esp]
	push	esi
	push	edi

; 556  :     int old_flush; /* value of flush param for previous deflate call */
; 557  :     deflate_state *s;
; 558  : 
; 559  :     if (strm == Z_NULL || strm->state == Z_NULL ||
; 560  :         flush > Z_FINISH || flush < 0) {

	mov	edi, DWORD PTR _strm$[esp+8]
	test	edi, edi
	je	$LN11@deflate
	mov	esi, DWORD PTR [edi+28]
	test	esi, esi
	je	$LN11@deflate
	cmp	ecx, 4
	ja	$LN11@deflate

; 561  :         return Z_STREAM_ERROR;
; 562  :     }
; 563  :     s = strm->state;
; 564  : 
; 565  :     if (strm->next_out == Z_NULL ||
; 566  :         (strm->next_in == Z_NULL && strm->avail_in != 0) ||

	cmp	DWORD PTR [edi+12], 0
	je	$LN13@deflate
	cmp	DWORD PTR [edi], 0
	jne	SHORT $LN14@deflate
	cmp	DWORD PTR [edi+4], 0
	jne	$LN13@deflate
$LN14@deflate:
	mov	eax, DWORD PTR [esi+4]
	cmp	eax, 666				; 0000029aH
	jne	SHORT $LN12@deflate
	cmp	ecx, 4
	jne	$LN13@deflate
$LN12@deflate:

; 569  :     }
; 570  :     if (strm->avail_out == 0) ERR_RETURN(strm, Z_BUF_ERROR);

	cmp	DWORD PTR [edi+16], 0
	push	ebx
	je	$LN120@deflate

; 571  : 
; 572  :     s->strm = strm; /* just in case */
; 573  :     old_flush = s->last_flush;

	mov	edx, DWORD PTR [esi+40]
	mov	DWORD PTR [esi], edi
	mov	DWORD PTR _old_flush$1$[esp+16], edx

; 574  :     s->last_flush = flush;

	mov	DWORD PTR [esi+40], ecx

; 575  : 
; 576  :     /* Write the header */
; 577  :     if (s->status == INIT_STATE) {

	cmp	eax, 42					; 0000002aH
	jne	$LN18@deflate

; 578  : #ifdef GZIP
; 579  :         if (s->wrap == 2) {

	cmp	DWORD PTR [esi+24], 2
	jne	$LN17@deflate

; 580  :             strm->adler = crc32(0L, Z_NULL, 0);

	push	0
	push	0
	push	0
	call	_crc32
	mov	DWORD PTR [edi+48], eax
	add	esp, 12					; 0000000cH

; 581  :             put_byte(s, 31);

	mov	eax, DWORD PTR [esi+20]
	mov	ecx, DWORD PTR [esi+8]
	mov	BYTE PTR [ecx+eax], 31			; 0000001fH
	inc	DWORD PTR [esi+20]
	mov	ecx, DWORD PTR [esi+20]

; 582  :             put_byte(s, 139);

	mov	eax, DWORD PTR [esi+8]
	mov	BYTE PTR [ecx+eax], 139			; 0000008bH
	inc	DWORD PTR [esi+20]
	mov	ecx, DWORD PTR [esi+20]

; 583  :             put_byte(s, 8);

	mov	eax, DWORD PTR [esi+8]
	mov	BYTE PTR [ecx+eax], 8
	mov	edx, DWORD PTR [esi+20]

; 584  :             if (s->gzhead == NULL) {

	mov	ebx, DWORD PTR [esi+28]
	inc	edx
	mov	DWORD PTR [esi+20], edx
	test	ebx, ebx
	jne	$LN19@deflate

; 585  :                 put_byte(s, 0);

	mov	eax, DWORD PTR [esi+8]
	mov	BYTE PTR [edx+eax], bl
	inc	DWORD PTR [esi+20]
	mov	ecx, DWORD PTR [esi+20]

; 586  :                 put_byte(s, 0);

	mov	eax, DWORD PTR [esi+8]
	mov	BYTE PTR [ecx+eax], bl
	inc	DWORD PTR [esi+20]
	mov	ecx, DWORD PTR [esi+20]

; 587  :                 put_byte(s, 0);

	mov	eax, DWORD PTR [esi+8]
	mov	BYTE PTR [ecx+eax], bl
	inc	DWORD PTR [esi+20]
	mov	ecx, DWORD PTR [esi+20]

; 588  :                 put_byte(s, 0);

	mov	eax, DWORD PTR [esi+8]
	mov	BYTE PTR [ecx+eax], bl
	inc	DWORD PTR [esi+20]
	mov	ecx, DWORD PTR [esi+20]

; 589  :                 put_byte(s, 0);

	mov	eax, DWORD PTR [esi+8]
	mov	BYTE PTR [ecx+eax], bl
	mov	edx, DWORD PTR [esi+20]

; 590  :                 put_byte(s, s->level == 9 ? 2 :

	mov	eax, DWORD PTR [esi+132]
	inc	edx
	mov	DWORD PTR [esi+20], edx
	cmp	eax, 9
	jne	SHORT $LN86@deflate
	mov	cl, 2
	jmp	SHORT $LN85@deflate
$LN86@deflate:
	cmp	DWORD PTR [esi+136], 2
	jge	SHORT $LN84@deflate
	cmp	eax, 2
	jl	SHORT $LN84@deflate
	xor	cl, cl
	jmp	SHORT $LN85@deflate
$LN84@deflate:
	mov	cl, 4
$LN85@deflate:
	mov	eax, DWORD PTR [esi+8]
	mov	BYTE PTR [edx+eax], cl
	inc	DWORD PTR [esi+20]
	mov	ecx, DWORD PTR [esi+20]

; 591  :                             (s->strategy >= Z_HUFFMAN_ONLY || s->level < 2 ?
; 592  :                              4 : 0));
; 593  :                 put_byte(s, OS_CODE);

	mov	eax, DWORD PTR [esi+8]
	mov	BYTE PTR [ecx+eax], 11			; 0000000bH

; 594  :                 s->status = BUSY_STATE;

	mov	eax, 113				; 00000071H
	inc	DWORD PTR [esi+20]

; 620  :             }
; 621  :         }

	mov	DWORD PTR [esi+4], eax
	jmp	$LN18@deflate
$LN19@deflate:

; 595  :             }
; 596  :             else {
; 597  :                 put_byte(s, (s->gzhead->text ? 1 : 0) +

	mov	ecx, DWORD PTR [ebx+36]
	mov	eax, DWORD PTR [ebx+44]
	neg	ecx
	sbb	cl, cl
	and	cl, 16					; 00000010H
	neg	eax
	sbb	al, al
	and	al, 2
	add	cl, al
	mov	eax, DWORD PTR [ebx+28]
	neg	eax
	sbb	al, al
	and	al, 8
	add	cl, al
	mov	eax, DWORD PTR [ebx+16]
	neg	eax
	sbb	al, al
	and	al, 4
	add	cl, al
	cmp	DWORD PTR [ebx], 0
	setne	al
	add	cl, al
	mov	eax, DWORD PTR [esi+8]
	mov	BYTE PTR [edx+eax], cl
	inc	DWORD PTR [esi+20]
	mov	edx, DWORD PTR [esi+20]

; 598  :                             (s->gzhead->hcrc ? 2 : 0) +
; 599  :                             (s->gzhead->extra == Z_NULL ? 0 : 4) +
; 600  :                             (s->gzhead->name == Z_NULL ? 0 : 8) +
; 601  :                             (s->gzhead->comment == Z_NULL ? 0 : 16)
; 602  :                         );
; 603  :                 put_byte(s, (Byte)(s->gzhead->time & 0xff));

	mov	eax, DWORD PTR [esi+28]
	mov	ecx, DWORD PTR [esi+8]
	movzx	eax, BYTE PTR [eax+4]
	mov	BYTE PTR [edx+ecx], al
	inc	DWORD PTR [esi+20]
	mov	edx, DWORD PTR [esi+20]

; 604  :                 put_byte(s, (Byte)((s->gzhead->time >> 8) & 0xff));

	mov	eax, DWORD PTR [esi+28]
	mov	ecx, DWORD PTR [esi+8]
	movzx	eax, BYTE PTR [eax+5]
	mov	BYTE PTR [edx+ecx], al
	inc	DWORD PTR [esi+20]
	mov	edx, DWORD PTR [esi+20]

; 605  :                 put_byte(s, (Byte)((s->gzhead->time >> 16) & 0xff));

	mov	eax, DWORD PTR [esi+28]
	mov	ecx, DWORD PTR [esi+8]
	movzx	eax, BYTE PTR [eax+6]
	mov	BYTE PTR [edx+ecx], al
	inc	DWORD PTR [esi+20]

; 606  :                 put_byte(s, (Byte)((s->gzhead->time >> 24) & 0xff));

	mov	eax, DWORD PTR [esi+28]
	mov	edx, DWORD PTR [esi+20]
	mov	ecx, DWORD PTR [esi+8]
	movzx	eax, BYTE PTR [eax+7]
	mov	BYTE PTR [edx+ecx], al
	mov	edx, DWORD PTR [esi+20]

; 607  :                 put_byte(s, s->level == 9 ? 2 :

	mov	eax, DWORD PTR [esi+132]
	inc	edx
	mov	DWORD PTR [esi+20], edx
	cmp	eax, 9
	jne	SHORT $LN90@deflate
	mov	cl, 2
	jmp	SHORT $LN89@deflate
$LN90@deflate:
	cmp	DWORD PTR [esi+136], 2
	jge	SHORT $LN88@deflate
	cmp	eax, 2
	jl	SHORT $LN88@deflate
	xor	cl, cl
	jmp	SHORT $LN89@deflate
$LN88@deflate:
	mov	cl, 4
$LN89@deflate:
	mov	eax, DWORD PTR [esi+8]
	mov	BYTE PTR [edx+eax], cl
	inc	DWORD PTR [esi+20]

; 608  :                             (s->strategy >= Z_HUFFMAN_ONLY || s->level < 2 ?
; 609  :                              4 : 0));
; 610  :                 put_byte(s, s->gzhead->os & 0xff);

	mov	eax, DWORD PTR [esi+28]
	mov	edx, DWORD PTR [esi+20]
	mov	ecx, DWORD PTR [esi+8]
	mov	al, BYTE PTR [eax+12]
	mov	BYTE PTR [edx+ecx], al
	mov	edx, DWORD PTR [esi+20]

; 611  :                 if (s->gzhead->extra != NULL) {

	mov	eax, DWORD PTR [esi+28]
	inc	edx
	mov	DWORD PTR [esi+20], edx
	cmp	DWORD PTR [eax+16], 0
	je	SHORT $LN21@deflate

; 612  :                     put_byte(s, s->gzhead->extra_len & 0xff);

	movzx	eax, BYTE PTR [eax+20]
	mov	ecx, DWORD PTR [esi+8]
	mov	BYTE PTR [edx+ecx], al
	inc	DWORD PTR [esi+20]

; 613  :                     put_byte(s, (s->gzhead->extra_len >> 8) & 0xff);

	mov	eax, DWORD PTR [esi+28]
	mov	edx, DWORD PTR [esi+20]
	mov	ecx, DWORD PTR [esi+8]
	movzx	eax, BYTE PTR [eax+21]
	mov	BYTE PTR [edx+ecx], al
	inc	DWORD PTR [esi+20]
	mov	edx, DWORD PTR [esi+20]
	mov	eax, DWORD PTR [esi+28]
$LN21@deflate:

; 614  :                 }
; 615  :                 if (s->gzhead->hcrc)

	cmp	DWORD PTR [eax+44], 0
	je	SHORT $LN22@deflate

; 616  :                     strm->adler = crc32(strm->adler, s->pending_buf,

	push	edx
	push	DWORD PTR [esi+8]
	push	DWORD PTR [edi+48]
	call	_crc32
	add	esp, 12					; 0000000cH
	mov	DWORD PTR [edi+48], eax
$LN22@deflate:

; 617  :                                         s->pending);
; 618  :                 s->gzindex = 0;
; 619  :                 s->status = EXTRA_STATE;

	mov	eax, 69					; 00000045H
	mov	DWORD PTR [esi+32], 0

; 620  :             }
; 621  :         }

	mov	DWORD PTR [esi+4], eax
	jmp	$LN18@deflate
$LN17@deflate:

; 622  :         else
; 623  : #endif
; 624  :         {
; 625  :             uInt header = (Z_DEFLATED + ((s->w_bits-8)<<4)) << 8;

	mov	eax, DWORD PTR [esi+48]
	shl	eax, 12					; 0000000cH
	sub	eax, 30720				; 00007800H

; 626  :             uInt level_flags;
; 627  : 
; 628  :             if (s->strategy >= Z_HUFFMAN_ONLY || s->level < 2)

	cmp	DWORD PTR [esi+136], 2
	jge	SHORT $LN25@deflate
	mov	ecx, DWORD PTR [esi+132]
	cmp	ecx, 2
	jl	SHORT $LN25@deflate

; 630  :             else if (s->level < 6)

	cmp	ecx, 6
	jge	SHORT $LN26@deflate

; 631  :                 level_flags = 1;

	mov	edx, 64					; 00000040H
	lea	ecx, DWORD PTR [edx+32]
	jmp	SHORT $LN29@deflate
$LN26@deflate:

; 632  :             else if (s->level == 6)

	jne	SHORT $LN28@deflate

; 633  :                 level_flags = 2;

	mov	edx, 128				; 00000080H
	lea	ecx, DWORD PTR [edx+32]
	jmp	SHORT $LN29@deflate
$LN28@deflate:

; 634  :             else
; 635  :                 level_flags = 3;

	mov	edx, 192				; 000000c0H
	lea	ecx, DWORD PTR [edx+32]
	jmp	SHORT $LN29@deflate
$LN25@deflate:

; 629  :                 level_flags = 0;

	xor	edx, edx
	mov	ecx, 32					; 00000020H
$LN29@deflate:

; 636  :             header |= (level_flags << 6);
; 637  :             if (s->strstart != 0) header |= PRESET_DICT;
; 638  :             header += 31 - (header % 31);
; 639  : 
; 640  :             s->status = BUSY_STATE;

	cmp	DWORD PTR [esi+108], 0
	mov	DWORD PTR [esi+4], 113			; 00000071H
	cmove	ecx, edx
	or	ecx, eax
	mov	eax, 138547333				; 08421085H
	mul	ecx
	sub	ecx, edx
	shr	ecx, 1
	add	ecx, edx
	shr	ecx, 4
	inc	ecx
	mov	eax, ecx
	shl	eax, 5
	sub	eax, ecx

; 641  :             putShortMSB(s, header);

	push	eax
	push	esi
	call	_putShortMSB
	add	esp, 8

; 642  : 
; 643  :             /* Save the adler32 of the preset dictionary: */
; 644  :             if (s->strstart != 0) {

	cmp	DWORD PTR [esi+108], 0
	je	SHORT $LN31@deflate

; 645  :                 putShortMSB(s, (uInt)(strm->adler >> 16));

	movzx	eax, WORD PTR [edi+50]
	push	eax
	push	esi
	call	_putShortMSB

; 646  :                 putShortMSB(s, (uInt)(strm->adler & 0xffff));

	movzx	eax, WORD PTR [edi+48]
	push	eax
	push	esi
	call	_putShortMSB
	add	esp, 16					; 00000010H
$LN31@deflate:

; 647  :             }
; 648  :             strm->adler = adler32(0L, Z_NULL, 0);

	push	0
	push	0
	push	0
	call	_adler32
	add	esp, 12					; 0000000cH
	mov	DWORD PTR [edi+48], eax
$LN18@deflate:

; 649  :         }
; 650  :     }
; 651  : #ifdef GZIP
; 652  :     if (s->status == EXTRA_STATE) {

	mov	eax, DWORD PTR [esi+4]
	push	ebp
	cmp	eax, 69					; 00000045H
	jne	$LN34@deflate

; 653  :         if (s->gzhead->extra != NULL) {

	mov	ecx, DWORD PTR [esi+28]
	cmp	DWORD PTR [ecx+16], 0
	je	$LN33@deflate

; 654  :             uInt beg = s->pending;  /* start of bytes to update crc */
; 655  : 
; 656  :             while (s->gzindex < (s->gzhead->extra_len & 0xffff)) {

	movzx	eax, WORD PTR [ecx+20]
	mov	ebp, DWORD PTR [esi+20]
	mov	ebx, ebp
	cmp	DWORD PTR [esi+32], eax
	jae	SHORT $LN122@deflate
$LL2@deflate:

; 657  :                 if (s->pending == s->pending_buf_size) {

	cmp	ebx, DWORD PTR [esi+12]
	jne	SHORT $LN102@deflate

; 658  :                     if (s->gzhead->hcrc && s->pending > beg)

	cmp	DWORD PTR [ecx+44], 0
	je	SHORT $LN36@deflate
	cmp	ebx, ebp
	jbe	SHORT $LN36@deflate

; 659  :                         strm->adler = crc32(strm->adler, s->pending_buf + beg,

	mov	eax, DWORD PTR [esi+8]
	sub	ebx, ebp
	push	ebx
	add	eax, ebp
	push	eax
	push	DWORD PTR [edi+48]
	call	_crc32
	add	esp, 12					; 0000000cH
	mov	DWORD PTR [edi+48], eax
$LN36@deflate:

; 660  :                                             s->pending - beg);
; 661  :                     flush_pending(strm);

	push	edi
	call	_flush_pending

; 662  :                     beg = s->pending;

	mov	ebp, DWORD PTR [esi+20]
	add	esp, 4

; 663  :                     if (s->pending == s->pending_buf_size)

	mov	ebx, ebp
	cmp	ebp, DWORD PTR [esi+12]
	je	SHORT $LN122@deflate
$LN102@deflate:

; 664  :                         break;
; 665  :                 }
; 666  :                 put_byte(s, s->gzhead->extra[s->gzindex]);

	mov	eax, DWORD PTR [esi+28]
	mov	ecx, DWORD PTR [esi+8]
	mov	edx, DWORD PTR [eax+16]
	mov	eax, DWORD PTR [esi+32]
	mov	al, BYTE PTR [edx+eax]
	mov	BYTE PTR [ebx+ecx], al
	mov	ebx, DWORD PTR [esi+20]

; 667  :                 s->gzindex++;

	inc	DWORD PTR [esi+32]
	inc	ebx
	mov	DWORD PTR [esi+20], ebx
	mov	ecx, DWORD PTR [esi+28]
	movzx	eax, WORD PTR [ecx+20]
	cmp	DWORD PTR [esi+32], eax
	jb	SHORT $LL2@deflate
$LN122@deflate:

; 668  :             }
; 669  :             if (s->gzhead->hcrc && s->pending > beg)

	mov	ecx, DWORD PTR [esi+28]
	cmp	DWORD PTR [ecx+44], 0
	je	SHORT $LN38@deflate
	cmp	ebx, ebp
	jbe	SHORT $LN38@deflate

; 670  :                 strm->adler = crc32(strm->adler, s->pending_buf + beg,

	mov	eax, DWORD PTR [esi+8]
	sub	ebx, ebp
	push	ebx
	add	eax, ebp
	push	eax
	push	DWORD PTR [edi+48]
	call	_crc32
	mov	DWORD PTR [edi+48], eax
	add	esp, 12					; 0000000cH
	mov	ecx, DWORD PTR [esi+28]
$LN38@deflate:

; 671  :                                     s->pending - beg);
; 672  :             if (s->gzindex == s->gzhead->extra_len) {

	mov	eax, DWORD PTR [esi+32]
	cmp	eax, DWORD PTR [ecx+20]
	jne	$LN103@deflate

; 673  :                 s->gzindex = 0;

	mov	DWORD PTR [esi+32], 0
$LN33@deflate:

; 677  :         else
; 678  :             s->status = NAME_STATE;
; 679  :     }
; 680  :     if (s->status == NAME_STATE) {

	mov	eax, 73					; 00000049H
	mov	DWORD PTR [esi+4], eax
$LN34@deflate:
	cmp	eax, 73					; 00000049H
	jne	$LN42@deflate

; 681  :         if (s->gzhead->name != NULL) {

	mov	eax, DWORD PTR [esi+28]
	cmp	DWORD PTR [eax+28], 0
	je	$LN41@deflate

; 682  :             uInt beg = s->pending;  /* start of bytes to update crc */

	mov	ebp, DWORD PTR [esi+20]
	mov	ebx, ebp
$LL6@deflate:

; 683  :             int val;
; 684  : 
; 685  :             do {
; 686  :                 if (s->pending == s->pending_buf_size) {

	cmp	ebx, DWORD PTR [esi+12]
	jne	SHORT $LN104@deflate

; 687  :                     if (s->gzhead->hcrc && s->pending > beg)

	mov	eax, DWORD PTR [esi+28]
	cmp	DWORD PTR [eax+44], 0
	je	SHORT $LN44@deflate
	cmp	ebx, ebp
	jbe	SHORT $LN44@deflate

; 688  :                         strm->adler = crc32(strm->adler, s->pending_buf + beg,

	mov	eax, DWORD PTR [esi+8]
	sub	ebx, ebp
	push	ebx
	add	eax, ebp
	push	eax
	push	DWORD PTR [edi+48]
	call	_crc32
	add	esp, 12					; 0000000cH
	mov	DWORD PTR [edi+48], eax
$LN44@deflate:

; 689  :                                             s->pending - beg);
; 690  :                     flush_pending(strm);

	push	edi
	call	_flush_pending

; 691  :                     beg = s->pending;

	mov	ebp, DWORD PTR [esi+20]
	add	esp, 4

; 692  :                     if (s->pending == s->pending_buf_size) {

	mov	ebx, ebp
	cmp	ebp, DWORD PTR [esi+12]
	je	SHORT $LN96@deflate
$LN104@deflate:

; 694  :                         break;
; 695  :                     }
; 696  :                 }
; 697  :                 val = s->gzhead->name[s->gzindex++];

	mov	eax, DWORD PTR [esi+28]
	mov	ecx, DWORD PTR [esi+32]
	mov	eax, DWORD PTR [eax+28]
	mov	dl, BYTE PTR [ecx+eax]
	movzx	eax, dl
	mov	DWORD PTR _val$1$[esp+16], eax
	lea	eax, DWORD PTR [ecx+1]
	mov	DWORD PTR [esi+32], eax

; 698  :                 put_byte(s, val);

	mov	eax, DWORD PTR [esi+8]
	mov	BYTE PTR [ebx+eax], dl
	mov	ebx, DWORD PTR [esi+20]
	inc	ebx

; 699  :             } while (val != 0);

	cmp	DWORD PTR _val$1$[esp+16], 0
	mov	DWORD PTR [esi+20], ebx
	jne	SHORT $LL6@deflate
	jmp	SHORT $LN5@deflate
$LN103@deflate:

; 674  :                 s->status = NAME_STATE;

	mov	eax, DWORD PTR [esi+4]

; 675  :             }
; 676  :         }

	jmp	$LN34@deflate
$LN96@deflate:

; 693  :                         val = 1;

	mov	DWORD PTR _val$1$[esp+16], 1
$LN5@deflate:

; 700  :             if (s->gzhead->hcrc && s->pending > beg)

	mov	eax, DWORD PTR [esi+28]
	cmp	DWORD PTR [eax+44], 0
	je	SHORT $LN46@deflate
	cmp	ebx, ebp
	jbe	SHORT $LN46@deflate

; 701  :                 strm->adler = crc32(strm->adler, s->pending_buf + beg,

	mov	eax, DWORD PTR [esi+8]
	sub	ebx, ebp
	push	ebx
	add	eax, ebp
	push	eax
	push	DWORD PTR [edi+48]
	call	_crc32
	add	esp, 12					; 0000000cH
	mov	DWORD PTR [edi+48], eax
$LN46@deflate:

; 702  :                                     s->pending - beg);
; 703  :             if (val == 0) {

	cmp	DWORD PTR _val$1$[esp+16], 0
	jne	$LN105@deflate

; 704  :                 s->gzindex = 0;

	mov	DWORD PTR [esi+32], 0
$LN41@deflate:

; 708  :         else
; 709  :             s->status = COMMENT_STATE;
; 710  :     }
; 711  :     if (s->status == COMMENT_STATE) {

	mov	eax, 91					; 0000005bH
	mov	DWORD PTR [esi+4], eax
$LN42@deflate:
	cmp	eax, 91					; 0000005bH
	jne	$LN50@deflate

; 712  :         if (s->gzhead->comment != NULL) {

	mov	eax, DWORD PTR [esi+28]
	cmp	DWORD PTR [eax+36], 0
	je	$LN49@deflate

; 713  :             uInt beg = s->pending;  /* start of bytes to update crc */

	mov	ebp, DWORD PTR [esi+20]
	mov	ebx, ebp
$LL9@deflate:

; 714  :             int val;
; 715  : 
; 716  :             do {
; 717  :                 if (s->pending == s->pending_buf_size) {

	cmp	ebx, DWORD PTR [esi+12]
	jne	SHORT $LN106@deflate

; 718  :                     if (s->gzhead->hcrc && s->pending > beg)

	mov	eax, DWORD PTR [esi+28]
	cmp	DWORD PTR [eax+44], 0
	je	SHORT $LN52@deflate
	cmp	ebx, ebp
	jbe	SHORT $LN52@deflate

; 719  :                         strm->adler = crc32(strm->adler, s->pending_buf + beg,

	mov	eax, DWORD PTR [esi+8]
	sub	ebx, ebp
	push	ebx
	add	eax, ebp
	push	eax
	push	DWORD PTR [edi+48]
	call	_crc32
	add	esp, 12					; 0000000cH
	mov	DWORD PTR [edi+48], eax
$LN52@deflate:

; 720  :                                             s->pending - beg);
; 721  :                     flush_pending(strm);

	push	edi
	call	_flush_pending

; 722  :                     beg = s->pending;

	mov	ebp, DWORD PTR [esi+20]
	add	esp, 4

; 723  :                     if (s->pending == s->pending_buf_size) {

	mov	ebx, ebp
	cmp	ebp, DWORD PTR [esi+12]
	je	SHORT $LN97@deflate
$LN106@deflate:

; 725  :                         break;
; 726  :                     }
; 727  :                 }
; 728  :                 val = s->gzhead->comment[s->gzindex++];

	mov	eax, DWORD PTR [esi+28]
	mov	ecx, DWORD PTR [esi+32]
	mov	eax, DWORD PTR [eax+36]
	mov	dl, BYTE PTR [ecx+eax]
	movzx	eax, dl
	mov	DWORD PTR _val$1$[esp+16], eax
	lea	eax, DWORD PTR [ecx+1]
	mov	DWORD PTR [esi+32], eax

; 729  :                 put_byte(s, val);

	mov	eax, DWORD PTR [esi+8]
	mov	BYTE PTR [ebx+eax], dl
	mov	ebx, DWORD PTR [esi+20]
	inc	ebx

; 730  :             } while (val != 0);

	cmp	DWORD PTR _val$1$[esp+16], 0
	mov	DWORD PTR [esi+20], ebx
	jne	SHORT $LL9@deflate
	jmp	SHORT $LN8@deflate
$LN105@deflate:

; 705  :                 s->status = COMMENT_STATE;

	mov	eax, DWORD PTR [esi+4]

; 706  :             }
; 707  :         }

	jmp	$LN42@deflate
$LN97@deflate:

; 724  :                         val = 1;

	mov	DWORD PTR _val$1$[esp+16], 1
$LN8@deflate:

; 731  :             if (s->gzhead->hcrc && s->pending > beg)

	mov	eax, DWORD PTR [esi+28]
	cmp	DWORD PTR [eax+44], 0
	je	SHORT $LN54@deflate
	cmp	ebx, ebp
	jbe	SHORT $LN54@deflate

; 732  :                 strm->adler = crc32(strm->adler, s->pending_buf + beg,

	mov	eax, DWORD PTR [esi+8]
	sub	ebx, ebp
	push	ebx
	add	eax, ebp
	push	eax
	push	DWORD PTR [edi+48]
	call	_crc32
	add	esp, 12					; 0000000cH
	mov	DWORD PTR [edi+48], eax
$LN54@deflate:

; 733  :                                     s->pending - beg);
; 734  :             if (val == 0)

	cmp	DWORD PTR _val$1$[esp+16], 0
	jne	$LN107@deflate

; 735  :                 s->status = HCRC_STATE;

	mov	DWORD PTR [esi+4], 103			; 00000067H
	mov	eax, 103				; 00000067H
$LN50@deflate:

; 739  :     }
; 740  :     if (s->status == HCRC_STATE) {

	cmp	eax, 103				; 00000067H
	jne	SHORT $LN58@deflate
$LN115@deflate:

; 741  :         if (s->gzhead->hcrc) {

	mov	eax, DWORD PTR [esi+28]
	cmp	DWORD PTR [eax+44], 0
	je	SHORT $LN57@deflate

; 742  :             if (s->pending + 2 > s->pending_buf_size)

	mov	edx, DWORD PTR [esi+20]
	mov	ecx, DWORD PTR [esi+12]
	lea	eax, DWORD PTR [edx+2]
	cmp	eax, ecx
	jbe	SHORT $LN59@deflate

; 743  :                 flush_pending(strm);

	push	edi
	call	_flush_pending
	mov	edx, DWORD PTR [esi+20]
	add	esp, 4
	mov	ecx, DWORD PTR [esi+12]
$LN59@deflate:

; 744  :             if (s->pending + 2 <= s->pending_buf_size) {

	lea	eax, DWORD PTR [edx+2]
	cmp	eax, ecx
	ja	SHORT $LN58@deflate

; 745  :                 put_byte(s, (Byte)(strm->adler & 0xff));

	mov	ecx, DWORD PTR [esi+8]
	movzx	eax, BYTE PTR [edi+48]

; 746  :                 put_byte(s, (Byte)((strm->adler >> 8) & 0xff));
; 747  :                 strm->adler = crc32(0L, Z_NULL, 0);

	push	0
	push	0
	mov	BYTE PTR [edx+ecx], al
	inc	DWORD PTR [esi+20]
	mov	edx, DWORD PTR [esi+20]
	mov	ecx, DWORD PTR [esi+8]
	movzx	eax, BYTE PTR [edi+49]
	push	0
	mov	BYTE PTR [edx+ecx], al
	inc	DWORD PTR [esi+20]
	call	_crc32
	add	esp, 12					; 0000000cH
	mov	DWORD PTR [edi+48], eax
$LN57@deflate:

; 748  :                 s->status = BUSY_STATE;
; 749  :             }
; 750  :         }
; 751  :         else
; 752  :             s->status = BUSY_STATE;
; 753  :     }
; 754  : #endif
; 755  : 
; 756  :     /* Flush as much pending output as possible */
; 757  :     if (s->pending != 0) {

	mov	DWORD PTR [esi+4], 113			; 00000071H
$LN58@deflate:
	cmp	DWORD PTR [esi+20], 0
	pop	ebp
	je	$LN61@deflate

; 758  :         flush_pending(strm);

	push	edi
	call	_flush_pending
	add	esp, 4

; 759  :         if (strm->avail_out == 0) {

	cmp	DWORD PTR [edi+16], 0
	je	$LN121@deflate
	mov	ebx, DWORD PTR _flush$[esp+12]
$LN64@deflate:

; 775  :                flush != Z_FINISH) {
; 776  :         ERR_RETURN(strm, Z_BUF_ERROR);
; 777  :     }
; 778  : 
; 779  :     /* User must not provide more input after the first FINISH: */
; 780  :     if (s->status == FINISH_STATE && strm->avail_in != 0) {

	mov	eax, DWORD PTR [esi+4]
	cmp	eax, 666				; 0000029aH
	jne	SHORT $LN119@deflate
	cmp	DWORD PTR [edi+4], 0
	jne	$LN120@deflate
$LN119@deflate:

; 782  :     }
; 783  : 
; 784  :     /* Start a new block or continue the current one.
; 785  :      */
; 786  :     if (strm->avail_in != 0 || s->lookahead != 0 ||

	cmp	DWORD PTR [edi+4], 0
	jne	SHORT $LN67@deflate
	cmp	DWORD PTR [esi+116], 0
	jne	SHORT $LN67@deflate
	test	ebx, ebx
	je	$LN77@deflate
	cmp	eax, 666				; 0000029aH
	je	$LN77@deflate
$LN67@deflate:

; 787  :         (flush != Z_NO_FLUSH && s->status != FINISH_STATE)) {
; 788  :         block_state bstate;
; 789  : 
; 790  :         bstate = (*(configuration_table[s->level].func))(s, flush);

	mov	eax, DWORD PTR [esi+132]
	push	ebx
	push	esi
	lea	eax, DWORD PTR [eax+eax*2]
	mov	eax, DWORD PTR _configuration_table[eax*4+8]
	call	eax
	add	esp, 8

; 791  : 
; 792  :         if (bstate == finish_started || bstate == finish_done) {

	cmp	eax, 2
	je	SHORT $LN69@deflate
	cmp	eax, 3
	jne	SHORT $LN68@deflate
$LN69@deflate:

; 793  :             s->status = FINISH_STATE;

	mov	DWORD PTR [esi+4], 666			; 0000029aH
$LN68@deflate:

; 794  :         }
; 795  :         if (bstate == need_more || bstate == finish_started) {

	test	eax, eax
	je	$LN71@deflate
	cmp	eax, 2
	je	$LN71@deflate

; 800  :             /* If flush != Z_NO_FLUSH && avail_out == 0, the next call
; 801  :              * of deflate should use the same flush parameter to make sure
; 802  :              * that the flush is complete. So we don't have to output an
; 803  :              * empty block here, this will be done at next call. This also
; 804  :              * ensures that for a very small output buffer, we emit at most
; 805  :              * one empty block.
; 806  :              */
; 807  :         }
; 808  :         if (bstate == block_done) {

	cmp	eax, 1
	jne	$LN77@deflate

; 809  :             if (flush == Z_PARTIAL_FLUSH) {

	cmp	ebx, eax
	jne	SHORT $LN74@deflate

; 810  :                 _tr_align(s);

	push	esi
	call	__tr_align
	add	esp, 4

; 811  :             } else { /* FULL_FLUSH or SYNC_FLUSH */

	jmp	SHORT $LN76@deflate
$LN107@deflate:

; 735  :                 s->status = HCRC_STATE;

	mov	eax, DWORD PTR [esi+4]

; 736  :         }

	jmp	$LN50@deflate
$LN49@deflate:

; 737  :         else
; 738  :             s->status = HCRC_STATE;

	mov	DWORD PTR [esi+4], 103			; 00000067H

; 739  :     }
; 740  :     if (s->status == HCRC_STATE) {

	jmp	$LN115@deflate
$LN61@deflate:

; 760  :             /* Since avail_out is 0, deflate will be called again with
; 761  :              * more output space, but possibly with both pending and
; 762  :              * avail_in equal to zero. There won't be anything to do,
; 763  :              * but this is not an error situation so make sure we
; 764  :              * return OK instead of BUF_ERROR at next call of deflate:
; 765  :              */
; 766  :             s->last_flush = -1;
; 767  :             return Z_OK;
; 768  :         }
; 769  : 
; 770  :     /* Make sure there is something to do and avoid duplicate consecutive
; 771  :      * flushes. For repeated and useless calls with Z_FINISH, we keep
; 772  :      * returning Z_STREAM_END instead of Z_BUF_ERROR.
; 773  :      */
; 774  :     } else if (strm->avail_in == 0 && flush <= old_flush &&

	cmp	DWORD PTR [edi+4], 0
	mov	ebx, DWORD PTR _flush$[esp+12]
	jne	$LN64@deflate
	cmp	ebx, DWORD PTR _old_flush$1$[esp+16]
	jg	$LN64@deflate
	cmp	ebx, 4
	je	$LN64@deflate
$LN120@deflate:

; 781  :         ERR_RETURN(strm, Z_BUF_ERROR);

	mov	eax, DWORD PTR _z_errmsg+28
	pop	ebx
	mov	DWORD PTR [edi+24], eax
	mov	eax, -5					; fffffffbH
	pop	edi

; 856  : }

	pop	esi
	pop	ecx
	ret	0
$LN74@deflate:

; 812  :                 _tr_stored_block(s, (char*)0, 0L, 0);

	push	0
	push	0
	push	0
	push	esi
	call	__tr_stored_block
	add	esp, 16					; 00000010H

; 813  :                 /* For a full flush, this empty block will be recognized
; 814  :                  * as a special marker by inflate_sync().
; 815  :                  */
; 816  :                 if (flush == Z_FULL_FLUSH) {

	cmp	ebx, 3
	jne	SHORT $LN76@deflate

; 817  :                     CLEAR_HASH(s);             /* forget history */

	mov	eax, DWORD PTR [esi+68]
	xor	edx, edx
	mov	ecx, DWORD PTR [esi+76]
	mov	WORD PTR [eax+ecx*2-2], dx
	mov	eax, DWORD PTR [esi+76]
	lea	eax, DWORD PTR [eax*2-2]
	push	eax
	push	edx
	push	DWORD PTR [esi+68]
	call	_memset
	add	esp, 12					; 0000000cH
$LN76@deflate:

; 818  :                 }
; 819  :             }
; 820  :             flush_pending(strm);

	push	edi
	call	_flush_pending
	add	esp, 4

; 821  :             if (strm->avail_out == 0) {

	cmp	DWORD PTR [edi+16], 0
	je	$LN121@deflate
$LN77@deflate:

; 822  :               s->last_flush = -1; /* avoid BUF_ERROR at next call, see above */
; 823  :               return Z_OK;
; 824  :             }
; 825  :         }
; 826  :     }
; 827  :     Assert(strm->avail_out > 0, "bug2");
; 828  : 
; 829  :     if (flush != Z_FINISH) return Z_OK;

	cmp	ebx, 4
	jne	$LN72@deflate

; 830  :     if (s->wrap <= 0) return Z_STREAM_END;

	mov	eax, DWORD PTR [esi+24]
	test	eax, eax
	jg	SHORT $LN79@deflate
	lea	eax, DWORD PTR [ebx-3]
	pop	ebx
	pop	edi

; 856  : }

	pop	esi
	pop	ecx
	ret	0
$LN79@deflate:

; 831  : 
; 832  :     /* Write the trailer */
; 833  : #ifdef GZIP
; 834  :     if (s->wrap == 2) {

	cmp	eax, 2
	jne	$LN80@deflate

; 835  :         put_byte(s, (Byte)(strm->adler & 0xff));

	mov	ecx, DWORD PTR [esi+20]
	movzx	eax, BYTE PTR [edi+48]
	mov	edx, DWORD PTR [esi+8]
	mov	BYTE PTR [edx+ecx], al
	inc	DWORD PTR [esi+20]
	mov	edx, DWORD PTR [esi+20]

; 836  :         put_byte(s, (Byte)((strm->adler >> 8) & 0xff));

	movzx	eax, BYTE PTR [edi+49]
	mov	ecx, DWORD PTR [esi+8]
	mov	BYTE PTR [edx+ecx], al
	inc	DWORD PTR [esi+20]
	mov	edx, DWORD PTR [esi+20]

; 837  :         put_byte(s, (Byte)((strm->adler >> 16) & 0xff));

	movzx	eax, BYTE PTR [edi+50]
	mov	ecx, DWORD PTR [esi+8]
	mov	BYTE PTR [edx+ecx], al
	inc	DWORD PTR [esi+20]
	mov	edx, DWORD PTR [esi+20]

; 838  :         put_byte(s, (Byte)((strm->adler >> 24) & 0xff));

	movzx	eax, BYTE PTR [edi+51]
	mov	ecx, DWORD PTR [esi+8]
	mov	BYTE PTR [edx+ecx], al
	inc	DWORD PTR [esi+20]
	mov	edx, DWORD PTR [esi+20]

; 839  :         put_byte(s, (Byte)(strm->total_in & 0xff));

	movzx	eax, BYTE PTR [edi+8]
	mov	ecx, DWORD PTR [esi+8]
	mov	BYTE PTR [edx+ecx], al
	inc	DWORD PTR [esi+20]
	mov	edx, DWORD PTR [esi+20]

; 840  :         put_byte(s, (Byte)((strm->total_in >> 8) & 0xff));

	mov	ecx, DWORD PTR [esi+8]
	movzx	eax, BYTE PTR [edi+9]
	mov	BYTE PTR [edx+ecx], al
	inc	DWORD PTR [esi+20]
	mov	edx, DWORD PTR [esi+20]

; 841  :         put_byte(s, (Byte)((strm->total_in >> 16) & 0xff));

	mov	ecx, DWORD PTR [esi+8]
	movzx	eax, BYTE PTR [edi+10]
	mov	BYTE PTR [edx+ecx], al
	inc	DWORD PTR [esi+20]
	mov	edx, DWORD PTR [esi+20]

; 842  :         put_byte(s, (Byte)((strm->total_in >> 24) & 0xff));

	mov	ecx, DWORD PTR [esi+8]
	movzx	eax, BYTE PTR [edi+11]
	mov	BYTE PTR [edx+ecx], al
	inc	DWORD PTR [esi+20]

; 843  :     }

	jmp	SHORT $LN81@deflate
$LN80@deflate:

; 844  :     else
; 845  : #endif
; 846  :     {
; 847  :         putShortMSB(s, (uInt)(strm->adler >> 16));

	movzx	eax, WORD PTR [edi+50]
	push	eax
	push	esi
	call	_putShortMSB

; 848  :         putShortMSB(s, (uInt)(strm->adler & 0xffff));

	movzx	eax, WORD PTR [edi+48]
	push	eax
	push	esi
	call	_putShortMSB
	add	esp, 16					; 00000010H
$LN81@deflate:

; 849  :     }
; 850  :     flush_pending(strm);

	push	edi
	call	_flush_pending

; 851  :     /* If avail_out is zero, the application will call deflate again
; 852  :      * to flush the rest.
; 853  :      */
; 854  :     if (s->wrap > 0) s->wrap = -s->wrap; /* write the trailer only once! */

	mov	eax, DWORD PTR [esi+24]
	add	esp, 4
	test	eax, eax
	jle	SHORT $LN82@deflate
	neg	eax
	mov	DWORD PTR [esi+24], eax
$LN82@deflate:

; 855  :     return s->pending != 0 ? Z_OK : Z_STREAM_END;

	xor	eax, eax
	cmp	DWORD PTR [esi+20], eax
	pop	ebx
	pop	edi
	sete	al

; 856  : }

	pop	esi
	pop	ecx
	ret	0
$LN71@deflate:

; 796  :             if (strm->avail_out == 0) {

	cmp	DWORD PTR [edi+16], 0
	jne	SHORT $LN72@deflate
$LN121@deflate:

; 797  :                 s->last_flush = -1; /* avoid BUF_ERROR next call, see above */

	mov	DWORD PTR [esi+40], -1
$LN72@deflate:
	pop	ebx
	pop	edi

; 798  :             }
; 799  :             return Z_OK;

	xor	eax, eax

; 856  : }

	pop	esi
	pop	ecx
	ret	0
$LN13@deflate:

; 567  :         (s->status == FINISH_STATE && flush != Z_FINISH)) {
; 568  :         ERR_RETURN(strm, Z_STREAM_ERROR);

	mov	eax, DWORD PTR _z_errmsg+16
	mov	DWORD PTR [edi+24], eax
$LN11@deflate:

; 856  : }

	pop	edi
	mov	eax, -2					; fffffffeH
	pop	esi
	pop	ecx
	ret	0
_deflate ENDP
_TEXT	ENDS
END
